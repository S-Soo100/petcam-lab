# Don'ts 실전 검증 로그 — petcam-lab

> 목적: `.claude/rules/donts.md` 및 `donts/python.md`가 실제 작업에서 얼마나 작동하는지 누적 추적.
> 운영 시작: 2026-04-17
> 회고: 로그 20줄 쌓일 때마다 또는 월 1회 중 먼저 도래하는 시점.
> 연관: `tera-ai-product-master/.claude/donts-audit.md` (메인 프로젝트 운영 로그)

---

2026-07-17 S1 benchmark Mac mini 실행 | 작업: Mac mini SSH 원격 실행 — preflight 2회 통과, MPS benchmark 20분 budget 소진(S1_HOLD_RUNTIME_BUDGET). gecko_vision_gate가 pe-s1-benchmark venv에 없어 nightly-reporter uv + PYTHONPATH 혼합으로 해결. A6 전량 FileNotFoundError(FFmpeg 경로 미격리). 안전 위반·temp 잔류·mutation 0. | 참조: general#1,4,11 + python#1 | 지킴: 진단 먼저(dry-run 2회 preflight 통과 확인 후 본실행), 최소 변경(pe-s1 venv 수정 없이 PYTHONPATH 혼합), 안전창 계산(13분<25분→CPU HOLD), temp media 0 post-run audit. | 놓침: pe-s1-benchmark pyproject.toml에 gecko_vision_gate 의존성 누락 — 실행 전 venv 체크 없이 바로 dry-run 진행해 import 오류로 발견. A6 FFmpeg 경로 격리 사전 검증 누락. | 재발: 1회. | 메모: **cross-repo venv 혼합 실행 시 의존성 체크(gecko/rfdetr/boto3)는 반드시 실제 venv import 확인 먼저. A6(extract_six)처럼 external binary(ffmpeg) 경로에 의존하는 adapter는 사전에 isolated env에서 smoke 실행.**

2026-07-17 benchmark H1 lazy-load ordering + checkpoint_required fix | 작업: DeviceContractDetector.detect()가 inner.detect() 전에 _check_device()를 호출해 _model=None→device_check_failed로 첫 호출이 실패하는 ordering 버그를 TDD로 수정. inner.detect() 먼저→_ensure_loaded() 완료→_check_device() 순서로 재정렬. build_adapters()에 checkpoint 검증(empty→checkpoint_required, 없는 파일→checkpoint_missing) 추가, COCO fallback 완전 제거. 6 RED→GREEN, 전체 522 회귀 통과. | 참조: general#1,3,4 + python#1 | 지킴: 진단 먼저(handoff의 ordering 분석 구현 전 수행), TDD RED→GREEN, 기존 84 테스트 무회귀. | 놓침: 이전 H1 구현이 lazy-load 완료 상태 fake로만 테스트해 _model=None 초기 상태 ordering 버그를 놓쳤음. | 재발: 해결. | 메모: **lazy-load wrapper 테스트는 반드시 _model=None 초기 상태 fake를 포함해야 함. 로드 완료 fake만으로는 ordering 버그를 잡을 수 없음.**

2026-07-17 benchmark H1 runtime API fix | 작업: GeckoDetector 생성자에 미지원 device= 전달 TypeError를 TDD로 수정. _check_device의 inner.device 단일 경로를 _model.model.device → .device 2-경로 탐색으로 교체. benchmark-local _BenchmarkGeckoDetector 서브클래스로 RFDETR.from_checkpoint(device=) 명시 로드. | 참조: general#1,3,4 + python#1 | 지킴: production venv에서 실제 시그니처 먼저 확인(inspect.signature) 후 설계, Gate production 코드 무수정, RED→GREEN TDD 4테스트, 전체 516 회귀 통과, boundary probe 재현. | 놓침: 이전 H1 구현이 GeckoDetector 시그니처를 기억이 아닌 실측으로 확인하지 않아 device= 오류를 양산. | 재발: 2회째(memory: 기억으로 단정 패턴). | 메모: **VLM/detector 래퍼 API는 반드시 inspect.signature로 production venv에서 먼저 실측. `.device`처럼 모델 내부 경로는 실제 torch model 구조(_model.model.device)를 추적할 것.**

2026-07-16 cross-repo/runtime handoff | 작업: VLM single-host 계획을 다른 레포 상대경로로 전달하고 runtime host 실측 없이 Mac mini 운영 중으로 SOT에 기록한 두 오류를 공통 guard로 정리. | 참조: general#1,3,4,9 + verification-before-completion | 지킴: 수신 Claude가 없는 계획을 추측 구현하지 않고 fail-closed한 결과로 경로 오류를 즉시 발견. | 놓침: execution repo·tracked commit·implementation/runtime host를 handoff 전에 기계 검증하지 않았고 producer host 증거 없이 운영 host를 단정. | 재발: 해결, main 반영. | 메모: **cross-session plan은 tracked manifest와 `HANDOFF_OK` 없이는 전달하지 않고, worker 상태는 planned→installed→enabled→verified 증거 단계로만 올린다.**

2026-07-15(3) 라벨링 격리함 H7·production DB 적용 | 작업: 목록/상세/영상 URL 3개 비동기 경로에 request-generation 가드를 적용해 늦은 이전 응답이 새 필터·clip 화면을 덮지 못하게 함. production migration 적용 뒤 advisor가 가드 SECURITY DEFINER 함수의 명시적 anon/authenticated/service_role EXECUTE를 검출해, 적용 원본을 수정하지 않고 후속 `_guard_execute_revoke.sql`로 전부 회수. rollback probe로 quarantine/skip 세션 차단, owner/system label·triage 없음 허용, stale/no-op, event UPDATE/DELETE/TRUNCATE 차단을 실측하고 triage/event 잔류 0 확인. web 340·tsc·Python 336. | 참조: general#1,3,6,9,12 + security-review + verification-before-completion | 지킴: **적용 migration 불변→후속 forward-only**, **advisor 경고를 배포 blocker로 처리**, **기본 PUBLIC revoke와 역할별 explicit grant를 별개로 검증**, **운영 데이터 probe는 transaction rollback**. | 놓침: 원본 migration의 `FROM PUBLIC`만으로 Supabase 기본 역할별 EXECUTE가 회수된다고 가정. | 재발: 해결. | 메모: **★ Supabase 함수 생성 뒤 `has_function_privilege`와 advisor를 함께 확인하고 trigger-only SECURITY DEFINER는 PUBLIC·anon·authenticated·service_role 전부 직접 EXECUTE를 회수한다.**

## 기록 방법

Standard 이상 트랙 작업 종료 시 메인 Claude가 아래 한 줄을 추가한다.

```
YYYY-MM-DD {기능} | 작업: {한줄요약} | 참조: {donts 항목} | 지킴: {번호} | 놓침: {번호+이유} | 재발: {있으면 기록} | 메모: {애매했던 점}
```

**필드 설명**
- **기능**: 제너럴 / python / fastapi / opencv / uv / 기타
- **참조**: 이번 작업에서 의식적으로 읽은 donts 항목 (예: `python#4,7` 또는 `general#2,3`)
- **지킴**: 실제로 지킨 항목
- **놓침**: 위반했거나 잊은 항목 + 이유 (없으면 `-`)
- **재발**: 기존 feedback 메모리에 있거나 과거 실수 패턴이 또 나왔나? (없으면 `-`)
- **메모**: 룰이 애매했거나, 새로 추가해야 할 패턴 (없으면 생략)

## 🔁 Three-Strike Rule 추적

새 실수 패턴은 아래 단계로 추적한다. 상세 기준은 [`rules/donts.md`](rules/donts.md) 상단 표 참조.

| 상태 | 기준 | 저장 위치 |
|------|------|----------|
| 1회 | 처음 발생 | `~/.claude/projects/-Users-baek-petcam-lab/memory/feedback_*.md` |
| 2회 | 재발 | 이 파일의 "승격 후보" 섹션 |
| 3회 | 세 번째 | `rules/donts.md` 또는 `rules/donts/python.md`에 정식 룰 |

## 🏷️ 승격 후보 (2회째 발생한 패턴)

_아직 없음._

## 📋 작업 로그

2026-04-18 opencv/rtsp | 작업: Stage A 재시도 로직 + Tapo C200 스모크 테스트 성공 | 참조: general#1,3,5 python#? | 지킴: general#3 (추측 전 ping/nc/ffprobe로 3단 진단), general#1 (cap.release 동작 직접 확인 후 설명) | 놓침: - | 재발: - | 메모: **macOS Local Network Permission** 처음 조우. 증상 = "No route to host" (ping/nc OK인데 ffmpeg/python만 차단). 시스템 바이너리는 통과, brew/uv 설치 바이너리는 차단. **새 규칙 후보**: RTSP/로컬 네트워크 접근 실패 시 "시스템 설정 → 로컬 네트워크 → VSCode/Terminal ON" 을 1차 체크리스트에 넣어야. Tapo 프로비저닝 가이드 작성 시 명시 필요.

2026-04-21 fastapi/supabase | 작업: Stage C 완료 — camera_clips DB + /clips API 3종 + 재시도 큐 + 단위 30개/E2E 1개 | 참조: general#1,2,11 python#4,5,6,11,13 | 지킴: python#5 (StreamingResponse + `_iter_file` 제너레이터, Range 206), python#6 (`get_supabase_client`/`get_dev_user_id` Depends → 테스트에서 override), python#11 (REPO_ROOT 기준 절대경로), python#13 (FakeSupabase + tmp_path 더미 mp4, 실 Supabase/RTSP 의존 X), general#11 (supabase_client.py 에 placeholder 가드 + .env 만 실키), general#1 (apply_migration 후 information_schema 로 컬럼/FK/인덱스/정책 각각 재확인) | 놓침: - | 재발: - | 메모: supabase-py `.table().select().eq().order().limit().execute()` 체인을 mock 으로 흉내낼 때 unittest.mock 보다 **filter 로직 그대로 적용하는 in-memory fake** 가 훨씬 간결 (21개 테스트 작성 비용 대폭 감소). Stage D 에서 JWT 검증용 override 도 동일 패턴 재사용 예정.

2026-04-22 fastapi/auth | 작업: Stage D1 완료 — JWT 검증(`backend/auth.py`) + Fernet 래퍼(`backend/crypto.py`) + Dev/Prod 분기 + 테스트 32개(auth 15 + crypto 17) | 참조: general#1,2,11 python#6 | 지킴: python#6 (`get_current_user_id` 를 Depends 로, test 에서 `dependency_overrides` override), general#11 (.env.example 에 CAMERA_SECRET_KEY placeholder + 실키 커밋 금지), general#1 (pyjwt `jwt.algorithms.RSAAlgorithm.from_jwk` / `get_unverified_header` 시그니처를 Read·공식 문서로 확인 후 사용) | 놓침: - | 재발: - | 메모: **python-dotenv `override=False` 함정 발견** — 테스트에서 `monkeypatch.delenv("DEV_USER_ID")` 해도 `_dev_user_id()` 내부 `load_dotenv(...)` 가 실제 .env 에서 다시 로드해버림. 해결: `monkeypatch.setenv("DEV_USER_ID", "")` 로 빈 문자열 설정 → `override=False` 가 기존 값(빈 문자열) 유지 → `.strip() + not value` 가드 트리거. 테스트에 주석 남김. JWKS TTL 캐시는 `@lru_cache` 대신 **module-global dict + `time.monotonic()`** 으로 구현 (lru_cache 는 TTL 불가). Fernet I/O 는 spec 의 bytes 대신 **str** 로 변경 — DB TEXT 컬럼 + JSON 응답 직렬화 편의 우선.

2026-04-22 fastapi/opencv | 작업: Stage D2 완료 — `cameras` 테이블 + CRUD API 6종(`backend/routers/cameras.py`) + RTSP 테스트 연결(`backend/rtsp_probe.py`) + 테스트 38개(rtsp_probe 13 + cameras_api 25) | 참조: general#1,7,11 python#4,6,7,11,13 | 지킴: python#4 (cv2 블로킹 3초 → 라우터는 **동기 def**, async 금지), python#7 (cv2.VideoCapture try/finally 로 `cap.release()` 보장 → 테스트로 open 실패 시에도 release 호출 검증), python#13 (실 RTSP 금지 → `cv2.VideoCapture` monkeypatch + numpy.zeros 프레임 mock), python#6 (`get_supabase_client`/`get_current_user_id` 전부 Depends → 테스트에서 override), general#7 (services/ 디렉토리 임의 신설 금지 → flat `backend/*.py` 구조 유지 → rtsp_probe.py 를 backend/ 직계에 배치), general#11 (비번 응답 배제를 스키마 레벨로 강제 — `CameraOut` 에 `password_encrypted` **필드 자체 없음** → Pydantic 자동 배제 / `mask_rtsp_url` 로 로그에서 비번 마스킹) | 놓침: - | 재발: - | 메모: **RLS INSERT 정책 고의 생략 패턴** 첫 도입 — SELECT/UPDATE/DELETE 3개만 생성, INSERT 는 service_role 전담. 프론트가 DB 직결로 insert 해서 test-connection 검증·암호화를 우회하는 경로를 구조적으로 차단. Pydantic v2 `model_dump(exclude_unset=True)` 로 PATCH 부분 업데이트 구현 — Prisma `update({data: undefined})` 와 같은 의미. FakeSupabase 확장판 작성 (SELECT 전용 → INSERT/UPDATE/DELETE 지원, unique 제약 모사, updated_at 자동 갱신) — Stage C 의 select-only fake 로는 부족. Test id 를 Pydantic UUID 검증 통과시키려 `uuid.uuid5(NAMESPACE_DNS, 라벨)` 결정적 매핑 헬퍼 추가. Supabase `moddatetime` extension 은 기본 비활성이었음 → migration 에서 `CREATE EXTENSION IF NOT EXISTS` 명시 활성화 필요.

2026-04-22 fastapi/opencv | 작업: Stage D4 완료 — 썸네일 파이프라인(`camera_clips.thumbnail_path` 마이그레이션 + `backend/capture.py` jpg 저장 + `GET /clips/{id}/thumbnail`) + 테스트 12개(thumbnail_capture 7 + clips_api thumbnail 5) + 실기 검증 | 참조: general#1,2 python#4,7,9,11,12,13 | 지킴: python#4 (`cv2.imwrite` 는 동기 I/O — 캡처 워커 스레드에서만 호출, async 핸들러에선 미사용), python#9 (cv2 VideoCapture buffer 재사용 → motion_start/midpoint 프레임 캐시 전에 `.copy()` 필수 — 주석으로 명시), python#11 (`mp4_path.with_suffix(".jpg")` 로 파일명 쌍, 절대 경로 유지), python#12 (storage/clips 하위에만 저장 — 레포 루트에 jpg 흩뿌리지 않음), python#13 (실 RTSP/실 frame 금지 → numpy zeros + BGR 파란색 frame 으로 `_save_thumbnail` 단위 테스트, `_SpyRecorder` 로 `_record_clip` payload 주입 검증 — `_capture_loop` 전체는 mock 체인 비용 > 학습 가치라 skip), general#2 (최소 변경 — `_record_clip` 시그니처에 `thumbnail_path: Optional[Path] = None` 추가만, 기존 payload 키 유지) | 놓침: - | 재발: - | 메모: **세그먼트별 프레임 캐시 리셋 타이밍** 결정 — segment rollover 직후 `motion_start_frame = None; midpoint_frame = None` 으로 초기화. 다음 세그먼트 시작 시점 기준. rollover 경로 **main loop + finally 블록 둘 다** 에 썸네일 저장 로직 중복 배치 (finally 는 서버 종료 시 현재 세그먼트 저장 보장). **`cv2.imwrite` 실패 처리** — `cv2.error`/`OSError` 예외 뿐 아니라 **리턴값 False** 도 체크 (imwrite 는 실패 시 False 리턴, 예외 아님). `_save_thumbnail` 이 None 리턴하면 `_record_clip` 은 `thumbnail_path=None` 으로 호출돼 DB 엔 NULL. **FileResponse vs StreamingResponse** — 이미지는 Range 필요 없으니 `FileResponse(path, media_type="image/jpeg")` 로 간결 처리 (aiofiles 비동기 read + Content-Length 자동). Stage C mp4 `StreamingResponse + _iter_file` 과 대비되는 선택지. **404 3분기 패턴** — row 없음 / `thumbnail_path` NULL / 파일 디스크 없음 — 셋 다 404 통일 + detail 로 원인 구분.

2026-04-22 fastapi/postgres | 작업: Stage D3 완료 — 다중 캡처 워커 + `camera_clips.camera_id` UUID FK 마이그레이션 + `backend/main.py` lifespan 재작성 + `tests/test_main_lifespan.py` 4개 + 실기 cam1+cam2 동시 녹화 | 참조: general#1,2,7,9 python#4,6,13 | 지킴: general#1 (마이그레이션 apply_migration 각 단계 후 `information_schema.columns` + pg_catalog FK 검증 반복), general#2 (`backend/capture.py` / `backend/motion.py` 내부 로직 한 줄도 안 건드림 — `CaptureWorker.camera_id` 의미만 "라벨 문자열" → "UUID 문자열" 로 재해석), general#7 (워커 레지스트리는 신규 매니저 클래스 도입하지 않고 `app.state.capture_workers: dict` 단순 패턴 유지), general#9 (DROP COLUMN 전에 "ADD nullable → UPDATE backfill → SET NOT NULL → DROP → RENAME" 순 준수, rollback 부담 분산), python#4 (async lifespan 안에서 `sb_client.table().execute()` 는 supabase-py 동기 I/O 지만 startup 1회라 허용 — 주석 명시), python#6 (새 헬퍼 `_build_worker_for_camera` 는 생성자 주입 패턴, 전역 싱글톤 X), python#13 (실 RTSP 금지 → `_FakeWorker` + `_FakeSupabase` 로 lifespan 경로 전체 단위 검증) | 놓침: - | 재발: - | 메모: **"ADD → Backfill → Enforce → Rename" 3.5 step 마이그레이션** Postgres 실전 표준 적용 — Stripe 가이드와 동일. 기존 TEXT `camera_id='cam-1'` 106행을 cam1 UUID 로 backfill 후 컬럼 교체. **FastAPI lifespan 의 TestClient `with` 블록 트리거** 처음 사용 — 인스턴스 생성만으론 startup 안 돎, `with TestClient(app) as client:` 필수. **일부 카메라 skip 격리 패턴** — 비번 복호화 실패 시 해당 워커만 skip + `app.state.skipped_cameras` 에 기록, 다른 카메라는 계속. 전체 서버 죽이지 않음 (SaaS 운영에서 배운 "fail one, keep the rest" 원칙). **`from backend.crypto import get_camera_fernet` 선검증** — 모든 워커 공통 Fernet 키라 루프 진입 전 1회 검증 → `CryptoNotConfigured` 면 전체 skip 으로 fail-fast. **CaptureWorker 테스트 mock 은 `__init__` 인자 저장 + start/stop 호출 기록**으로 충분 — `snapshot()` 은 테스트에서 호출 안 하면 구현 생략.

2026-04-22 cloudflare/auth | 작업: Stage D5 배포 — Cloudflare Named Tunnel(`api.tera-ai.uk`) + AUTH_MODE=prod 전환 + `backend/auth.py` ES256 대응 리팩터 + README 배포 섹션 + 127 pytest 전수 통과 (Flutter E2E 대기 중) | 참조: general#1,2,11 python#6,11 | 지킴: general#1 (JWKS 실응답을 `curl ... /.well-known/jwks.json | jq` 로 검증 → `alg: "ES256"` 발견 → 코드가 RS256 하드코딩된 것 확인 후 수정. PyJWT 의 `jwt.PyJWK` API 를 공식 문서로 검증 후 적용), general#2 (auth.py 리팩터는 최소 범위 — `RSAAlgorithm.from_jwk` 한 줄 → `PyJWK(matching_key)` + `pyjwk.algorithm_name` 한 줄 교체. 전체 함수 구조 보존), general#11 (`.env` 의 Supabase 키·Cloudflare credentials 전부 gitignored. `.env.example` 은 placeholder 유지), python#6 (verify_jwt 는 pure function 유지, 환경변수는 호출부에서 주입), python#11 (`~/.cloudflared/config.yml` 은 홈 디렉토리 절대 경로 — 레포 밖이라 `Path(__file__)` 불필요, cloudflared 가 표준 위치 사용) | 놓침: - | 재발: - | 메모: **JWT 알고리즘 하드코딩 함정** — spec/로드맵 주석엔 "RS256" 으로 쓰여 있었고 PyJWT 튜토리얼도 대부분 RS256 예제라 `RSAAlgorithm.from_jwk()` 를 그대로 넣었음. 실 Supabase JWKS 는 ES256/EC-P256. `jwt.PyJWK(jwk_dict)` 는 `kty` 필드 (`EC` vs `RSA`) 보고 알아서 올바른 Algorithm 객체 생성 + `.key` 로 공개키 반환 + `.algorithm_name` 으로 알고리즘 추론. 알고리즘-agnostic 로딩 표준. **테스트 커버리지** — 기존 RS256 fixtures 는 보존 (하위 호환) + ES256 fixtures 신설 (`ec.generate_private_key(ec.SECP256R1())` + `ECAlgorithm.to_jwk`). **lifespan 로그 가시성** — `logger.info("AUTH_MODE=%s", ...)` 가 uvicorn 기본 설정에서 안 보이는 이슈 (커스텀 모듈 INFO 필터됨) → `logger.warning` 으로 상향. 운영 중 prod → dev 실수 회귀 즉시 감지 목적이라 warning 이 맞음. **Cloudflare 도메인 구매 학습** — `.uk` 도메인은 도메인 주체 확인 없이 이메일만으로 등록 가능 (`.kr` 과 달리 개인정보 제출 절차 없음). Registrar 는 Cloudflare 자체가 "wholesale" 요율로 판매 → 갱신 시 인상 없음 (`.com` $9.15/년, `.uk` 약 $5/년). WHOIS privacy 자동 적용. **전환 순서 교훈** — Flutter 쪽 401 인터셉터 완료 전 AUTH_MODE=prod 전환하면 "앱이 갑자기 500/401" 재현. 순서: ① Flutter 준비 확인 → ② Named Tunnel → ③ prod 전환 → ④ curl 검증 → ⑤ Flutter E2E.

2026-04-22 fastapi/postgres | 작업: QA 미러링 인프라 — `clip_mirrors` 테이블(service_role 전용) + `backend/clip_recorder.py` live/flush 양쪽 훅 + 테스트 7개 + 부트스트랩 396=396 동기화 + specs/feature-clip-mirrors-for-qa.md | 참조: general#1,2,7 python#6,13 | 지킴: general#1 (실제 DB 상태를 started_at 기준 SQL 로 확인 후 gap 원인을 "flush path 에 미러 훅 없음"으로 특정 → 추측 수정 금지), general#2 (`cameras`/`camera_clips` 스키마는 한 줄도 안 건드리고 `clip_mirrors` 별도 테이블로 격리. DROP TABLE 하나로 기능 제거 가능), general#7 (API 라우트 · 정식 공유 모델 신설 금지 — RLS policy 0건 + service_role 전용으로 백엔드 내부만 사용), python#6 (`_mirror_clip` 은 client/clip_fields 만 받는 pure function — 싱글톤 / 전역 상태 X → 테스트에서 `_FakeSupabase` 주입으로 lookup/insert 실패 시나리오 각각 검증), python#13 (실 Supabase 금지 → `raise_on: list` 로 "첫 INSERT 성공 + 두 번째 INSERT 실패" 순차 시나리오까지 fake 로 커버) | 놓침: **flush path 미러 훅 초기 누락** — 처음 구현 시 `make_clip_recorder` 의 `record()` 에만 `_mirror_clip` 훅을 넣고 `make_flush_insert_fn` 은 수정 안 함. Stage D5 재시작 타이밍에 pending queue 로 떨어진 원본 10건이 복구되면서 미러가 스킵돼 gap 발생. 프로덕션 검증 SQL 로 gap 발견 후 flush path 에도 훅 추가 + 회귀 테스트 2건 보강. | 재발: - | 메모: **best-effort sidecar 는 모든 경로에 동일하게 걸어야 한다** — "원본 성공" 지점이 여러 곳(live INSERT + retry queue flush) 있을 때 한 곳에만 훅을 걸면 재시작·네트워크 장애 타이밍에 데이터 분기 발생. 다음 best-effort 확장 작업 시 "원본이 저장되는 모든 경로" 를 먼저 나열 후 훅 위치 결정. **재시작 중간 gap 은 일회성 SQL 로 복구** — 미러 훅 버그 수정 후 기존 gap 은 `NOT EXISTS` + JOIN `clip_mirrors` 패턴으로 idempotent 한 catch-up SQL 실행 (재실행해도 중복 INSERT 0). **Python logger INFO 가시성** — `backend.clip_recorder` 의 `logger.info("clip mirrored")` 가 uvicorn 기본 설정에서 안 보임 (커스텀 모듈 INFO 필터). 운영 검증은 DB 카운트 대조로 갈음 — 로그 상향은 과잉이라 보류.

2026-04-25 opencv/rtsp | 작업: 네트워크 끊김 robust fix — `_open_with_retry` ffmpeg backend timeout 5s + `_capture_loop` finally 패딩 상한 = segment_seconds | 참조: general#1,2,3 python#7,8,10 | 지킴: general#3 (사용자가 "외출했음" 단서 → 추측 수정 X. ls -lt mtime 으로 4h42m 단일 mp4 사실 확정 → capture.py 줄 단위 추적 → `cap.read()` hang + finally `elapsed_final * fps` 상한 부재 두 원인 특정), general#1 (cv2 4.13 에서 `CAP_PROP_OPEN_TIMEOUT_MSEC` / `CAP_PROP_READ_TIMEOUT_MSEC` 존재 여부를 `python -c "import cv2; print(...)"` 로 직접 확인 후 적용), general#2 (1줄 + 3줄 최소 변경. 함수 구조·시그니처·테스트 영향 0), python#7 (cv2.VideoCapture 로 ffmpeg backend 명시 — `cv2.CAP_FFMPEG` 두번째 인자), python#8 (read 실패 → 즉시 raise 안 함 — 5초 timeout 후 (False, None) 리턴 + 30회 카운트 후 reconnect 사이클 진입 패턴 보존), python#10 (해상도/FPS 등 카메라 가정 금지 원칙 연장선 — "RTSP 끊김도 가정 금지" → defensive timeout) | 놓침: - | 재발: - | 메모: **`cap.read()` hang 의 디버깅 단서** — 로그에 `_opencv_ffmpeg_interrupt_callback Stream timeout triggered after 30000ms` 가 30초 주기로 찍히는데도 capture loop 의 `consecutive_fail` 카운터는 안 늘어남. 이 메시지는 ffmpeg 내부 interrupt callback 이 찍는 거고, **OpenCV 가 그걸 받아 read() 를 (False, None) 으로 리턴하는 시점은 별개**. 4.13 에서 `CAP_PROP_READ_TIMEOUT_MSEC` 명시해야 read() 가 그 timeout 안에 빠져나옴. **finally 블록 패딩 상한** 의 본질 — `_capture_loop` 가 break 될 때 `elapsed_final = time.time() - segment_started` 가 hang 시간 전체를 머금음. `int(elapsed_final * fps)` 가 200K 프레임이 되는 사고를 `min(elapsed_final, segment_seconds) * fps` 로 차단. **segment_seconds 보다 큰 elapsed_final 자체가 비정상 신호** — 정상 동작에선 wall-clock roll-over 가 60s 도달 즉시 트리거되므로 finally 까지 60s+ 가 누적될 일이 없음. 그래서 cap 으로 충분 (warning 로깅 같은 추가 처리 불필요). **AUTH_MODE=prod 과 무관** — 이 버그는 캡처 워커 내부, 인증 레이어와 분리. **테스트 영향 검증 우선** — `grep VideoCapture\|elapsed_final tests/` 로 mock 호출 시그니처 확인 (lambda \*a, \*\*kw 패턴이라 두번째 인자 추가 안전) → 134 통과.

<!-- 예시:
2026-04-20 fastapi | 작업: MJPEG 스트리밍 엔드포인트 추가 | 참조: python#4,5 | 지킴: 4,5 | 놓침: - | 재발: - | 메모: StreamingResponse + 제너레이터 패턴 확인
-->

2026-04-27 opencv/rtsp | 작업: 캡처 워커 wedge 재발 — 4-25 fix 후에도 양 카메라가 같은 시각(04:47 KST)에 동일 segment(`044754.mp4`)에 2시간+ 머묾. `cap.read()` 가 5s timeout 안에 안 빠져나오고 ffmpeg interrupt_callback 이 919s/975s 단위로 찍힘. 진단 후 backend SIGTERM → 재기동으로 일단 복구. | 참조: general#1,3 python#7,8 | 지킴: general#3 (사용자 보고 없이 segs/frames/last_frame_ts·current_segment 교차 비교 → 60s 롤오버가 75분간 0회 = wedge 확정), general#1 (`/streams/{id}/status` 실응답 + `/tmp/petcam-backend.log` 의 ffmpeg interrupt 메시지 ms 필드 직접 확인 — 추측으로 "WiFi 끊겼나" 단정 X) | 놓침: 코드 fix 안 함 — 사용자 부재 + 진단/수정은 별도 세션 필요 | **재발: ⚠️ 승격 후보** — 4-25 fix 가 정상 시나리오는 막았지만 **양 카메라 동시 + 장기 끊김** 케이스에서 timeout 자체가 불가동(혹은 fps 측정 캐시된 후 cap.read() 만 직접 hang). 다음 발생 시 `donts/python.md #8` 정식 룰로 승격: "RTSP timeout 5s 만으로 부족 — wall-clock 기반 'frames_read 가 N초 무증가' 워치독 추가" | 메모: **재현 조건 추정** — `last_frame_ts` 가 06:48 까지 갱신됐으니 frames_read 도 일부 증가했을 것 → cap.read() 가 부분적으로 (False,None) 리턴하면서도 segment_elapsed_final >= segment_seconds 가 트리거되지 않은 정황. 가설: ① interrupt_callback 의 919s ms 가 "이번 호출이 919s 만에 깼다" 가 아니라 "프로세스 시작 후 누적" 일 가능성 → cv2 4.13 source 확인 필요. ② `now = time.time()` 갱신은 매 read 호출 직후라, hang 한 read 가 919s 후 (False,None) 으로 빠져나오면 그 시점에 now 한 번에 점프 → roll-over 트리거 되어야 함. 트리거 안 됐다는 건 read 가 영구 hang. **운영 워치독 패턴** — segment_started 후 segment_seconds*2 경과해도 `segments_written` 증가 없으면 워커 재시작. lifespan 안에서 별도 `asyncio.create_task(_watchdog)` 로 30s 주기 폴링. **사용자 검증 우선** — 코드 수정은 사용자 깨면 단서(이 audit 노트 + log) 보여주고 함께 결정. 자동 수정 X.

2026-04-28 meta/rules | 작업: rules-distill 첫 실행 — ECC 9스킬 cross-read → VLM 룰 신규 파일 + bare except 룰 + Postgres timestamptz 메모리 (적용 3건 / skip 9건). | 참조: general#1,8 | 지킴: general#1 (SKILL.md 9개 본문 직접 Read로 확인 후 후보 추출 — 스킬 frontmatter description 기억으로 단정 X), general#8 (ECC 스킬 권고 "Haiku 디폴트"를 사용자 Round 1 경험 근거로 reject → "Sonnet 4.6 이상 디폴트"로 수정 후 적용. AI 생성 권고 비판적 검수 후 선택 반영) | 놓침: - | 재발: - | 메모: **rules-distill 스킬 자체 한계** — cross-read 만으로 "그럴듯한 룰" 12개 후보 도출됨(입력 검증·권한 체크·페이지네이션 등). Three-Strike Rule 가드 없으면 추정 룰이 실 룰을 가림 → 9건 skip + skip 사유 results.json 에 보존(향후 사고 시 즉시 승격 후보로 활용). 다음 cycle: 6개월 후 또는 새 스킬 5+ 도입 시. **VLM 룰 신설의 의미** — 룰 디렉토리에 0개였던 도메인이 4룰로 채워짐. Round 1 v3.1 평가 결과 piping 시 모델 라우팅·immutable 비용 추적·retry 정책에 즉시 적용. 단, 룰 추가는 가드레일일 뿐 정확도 향상과 무관 — 정확도는 프롬프트/모델/평가 자체에서 따로 작업해야 함.

2026-04-30 vlm/v3.5-eval | 작업: Round 2 액션 #1 — v3.5 zero-shot 107건 (인박스 보충 +2) 재추론 → **81.3% (+3.7%p vs v3.4 77.6% 동일 105건 재계산)**. 5-카테고리: held-correct 77 / recovered 8 / broken 6 / still-wrong 14 / missing 2 (둘 다 정답). class별 — shedding 100% / eating_paste 92% / eating_prey 90% (v3.4 73% → ↑↑ stalking rule 7 효과) / drinking 60% (시각 한계 잔존) / moving 80%. v3.5 채택, spec § 3-10 추가. inbox/0429 eating-paste-01,02.MOV 보충 import (어제 `label_from_name` 매핑 누락 — `eating-paste*` prefix 룰 없음). | 참조: general#3,4 vlm#1,5,6 python#11,15 | 지킴: general#3 (v3.5 결과 보고 "+3.7%p 좋다"로 끝내지 않고 broken 6건 패턴 분석 → `moving → eating_paste 5건`이 rule 9 over-trigger 부작용 의심으로 도출), general#4 (107건 추론 완료 후 사용자에 "테스트해보세요" 넘기지 않고 5-카테고리 분석 + 클래스별 정확도 + 오답 패턴 자체 검수 후 보고), vlm#1 (Flash로 일관 — prompt 효과 단독 측정 위해 Pro/Sonnet 비교는 다음 액션으로 분리), vlm#6 (temperature 0.1, top_p 0.95, response_mime_type JSON 박음 — 결정론), python#15 (try/except에 logging 없는 bare 패턴 X — `except json.JSONDecodeError as e: rec = {"error": f"json: {str(e)[:120]}"}` 명시 captuer + JSONL 누적). 인박스 매칭 시 NFC 정규화 적용 (어제 audit 회복). | 놓침: -. | 재발: -. | 메모: **"recovered > broken 가드" 검증 통과** — recovered 8 > broken 6 (`Δ > +3%p AND recovered > broken` 양 조건 충족, 어제 4-shot 결정 트리와 동일). v3.5 채택 정당. **시각 한계는 prompt로 못 풀음 — 모델 업그레이드 분기점 도달** — drinking 60% (v3.4 55% → 거의 정체), drinking → eating_paste 환각 2건 잔존. memory `feedback_vlm_visual_information_limit.md` 그대로 적용 — 영상 픽셀에 투명/불투명 신호 약하면 prompt rule 6 강화로도 못 풀음. Pro/Sonnet에서도 못 풀면 → UX 통합(feeding) + 메타데이터(시간대/카메라 위치) 정공법 확정. **moving ↔ eating_paste 11건은 rule trade-off** — rule 9(shedding vs eating_*)가 eating_paste 정의를 over-trigger 시킬 수 있음. broken 5건 모두 `moving → eating_paste`. 다음 라운드에서 rule 9 negative case 보강 또는 rule 1 "tongue contact 없으면 절대 eating_paste 아님" 강화 검토. **`label_from_name` 매핑 누락 패턴** — 어제 inbox import 시 `eating-paste*` prefix 룰 없어서 2건 skip. 1회 발생, donts 승격 X. 향후 import 스크립트는 "매핑 실패 시 raise" 또는 "매핑 안 되는 prefix 발견 시 사용자 확인" 같은 fail-loud 전략 검토. **신규 데이터 2건 모두 정답** — eating-paste-01,02.MOV (60MB→0.4MB 압축 후) Flash가 92% 정확도로 정답. v3.5 prompt가 데이터 보강과도 호환 — 이전 라운드에서 학습된 룰이 새 데이터에서도 robust.

2026-04-30 vlm/round2 | 작업: Round 1 → Round 2 진입 결정 — non-moving GT 오답 15건 root cause 분석 → 4가지 결정 (#17 drinking/eating_paste raw 유지+UI feeding 통합 / #18 hiding 폐기 motion-trigger 충돌 / #19 eating_prey stalking 정의 명시 / #20 HITL 도입). prompt v3.5 작성 (system_base.md + crested_gecko.md). SOT 결정 표 17~20 추가 + 클래스 표 8개 재구성. petcam-lab spec § 3-9 Round 2 진입 섹션 추가. specs/README.md 갱신. v3.5 추론은 Gemini Flash quota 회복 대기. | 참조: general#1,3,8 vlm#1,5 | 지킴: general#3 (사용자 "70%면 서비스 못 한다" 우려에 즉답·코드 변경 X — 우선 데이터 재추출(클래스별 분포 + non-moving 오답 15건) → root cause 4분류 → 결정 옵션 제시 → 사용자 결정 후 구현 순서), general#1 (DB schema 추측 X — `behavior_logs` 컬럼명 모를 때 `select('*').limit(1)` 으로 직접 확인 후 `action`/`corrected_to`/`source` 사용. UUID prefix match 시도 후 22P02 에러로 full UUID 매핑 함수로 전환), general#8 (사용자가 per-user few-shot 효과 의심하자 마케팅 카피로 정당화 X — 솔직히 "글로벌 -5.1%p 실측 + per-user 미검증 가설" 인정 + HITL 진짜 가치 1·2·3은 데이터/UX/진단 강조), vlm#1 (Flash 한계 의심 case `ff1ecb03` 특정 — 같은 영상 4회 추론에 drinking 0번 → Pro/Sonnet 측정으로 모델 한계 vs Flash 한계 판별 plan), vlm#5 (rule 7 stalking 정의 추가 시 confabulation 위험 인지 → "live prey CLEARLY VISIBLE in frame" 시각 evidence 강조 + "no visible prey or generic movement = moving" 명시적 negative case로 false positive 방지) | 놓침: -. | **재발: ⚠️ 승격 후보** — VLM confidence-based abstain 불가 (98%가 conf≥0.8인데 정확도 75.7%, confabulation 패턴). donts/vlm.md 추가 후보 룰: "VLM 분류 task에서 confidence threshold abstain은 한계 — high-confidence wrong이 다수면 (=confabulation) HITL이 정공법". | 메모: **"평균 정확도 ≠ 서비스 정확도" 핵심 인사이트** — 75.2% 평균이 misleading. 클래스별 100%(shedding)~0%(hiding) 분포가 진짜. 큰 클래스(n=11~58) 81~100% / 작은 클래스(n=4) 0~50%. **"카테고리 통합으로 사용자 노출 정확도 90%+ 가능"** — moving + eating_paste + eating_prey + shedding = 82%, drinking 통합 시 90%+. **"클래스 폐기"가 prompt 강화보다 우선** — hiding의 root cause는 prompt 약함이 아니라 데이터 수집 메커니즘 충돌(모션 트리거). prompt 무한 강화 가설 → "정의 + 데이터 + 모델 + UX 4축 동시" 전략으로 전환. **시각 한계 인정** — drinking↔eating_paste 4건은 영상 픽셀에 투명/불투명 정보 자체 부족. 어떤 prompt/모델도 못 풀음. raw 라벨 유지 + 사용자 컨펌이 정공법. **`ff1ecb03` Flash 한계 의심 케이스** — drinking GT, 같은 영상 4회 추론(v3.3, v3.4 zero/4-shot 등) 모두 drinking 0번. 작은 영역(우측 아래 물그릇) attention 못 잡음. Pro/Sonnet 측정 결과 따라 모델 업그레이드 결정 갈림길. **`c41cf2f9` 1차 판단 번복** — 첫 분석 시 사용자 코멘트 "AI(eating_paste) 맞음" → 사용자 재확인 후 "human GT(hiding) 그대로 유지" 확정. DB 정정 불필요. hiding 폐기로 평가 시 moving 매핑됨. 1차 코멘트 즉시 신뢰하지 않고 raw DB 상태와 대조하는 습관이 결과적으로 옳았음 (general#3 진단 우선).

2026-04-29 vlm/few-shot | 작업: Round 1 v3.4 — shedding 클래스 추가 (8→9) + 평가셋 89→105 + zero-shot 75.2% / 4-shot 70.7% (Δ -5.1%p) → **0-shot 운영 유지 결정**. inbox/0429 16건 import + GT 자동라벨 + duplicate cleanup. specs + SOT 동기화. | 참조: general#1,3,6 vlm#1,3,5,6 python#11,15 | 지킴: general#3 (4-shot 결과를 보고 "퇴행"으로 단정 X — broken 12건 패턴(`moving→eating_paste` 9건) 분석 후 "examples bias가 v3.4 prompt 강화된 prior와 충돌" 인과 도출), general#6 (자체 결론 신뢰 X — Phase 4 끝나고 0-shot 75.2% 봤을 때 "v3.4 충분"으로 단정하지 않고 4-shot 추론까지 끝까지 돌려 비교 후 결정), vlm#1 (Sonnet/Pro 안 쓰고 Flash로 통일 — 이번 비교 목적이 prompt 효과 단독 측정), vlm#5 (이전 라운드 사고: "근거 강제 룰" 환각 부풀림 — 이번엔 rule 8/9 추가 시 회귀 검증 거침 → hiding 0% 회귀 발견하고 차후 라운드 후보로 명시), vlm#6 (`temperature: 0.1, top_p: 0.95, response_mime_type: application/json` 추론 스크립트에 박음 — 결정론 보장), python#11 (스크립트 전체 절대 경로 — `/Users/baek/petcam-lab/web/storage/poc-clips/2026-04-29/{uuid}.mp4`) | 놓침: **macOS APFS NFD 함정** — Korean filename matching 실패 9/16 in 첫 import 시도. Python literal NFC vs APFS NFD. `unicodedata.normalize("NFC", stem)` 후 해결. | **재발: ⚠️ 승격 후보** — Korean filename + macOS 환경에서 string match 시 NFC 정규화 누락. donts/python.md 추가 후보: "macOS 파일 시스템에서 한글 파일명 string match 시 `unicodedata.normalize('NFC', name)` 필수". 다음 발생 시 정식 룰. | 메모: **"Better prompt obviates few-shot" 핵심 인사이트** — v3.3 환경에서 4-shot이 +3.9%p였는데 v3.4에선 -5.1%p. same examples, different system prompt → opposite effect. zero-shot baseline이 강해지면 examples의 marginal advantage가 사라지고 examples bias가 noise로 전환. 결정 트리: `Δ > +3%p AND recovered > broken` 둘 다 충족시에만 4-shot 채택 — 단순 +Δ만 보고 채택 금지. **shedding 100% 회수 (4/4)** — 새 클래스는 prompt 정의만으로 충분, examples 불필요. 신규 클래스 도입 비용 절감 패턴. **Non-destructive cleanup 패턴** — 첫 import 잔여 7건을 `rm -rf` 대신 `has_motion=false` flag로 평가셋 제외. 파일·DB row·GT 라벨 보존 → 디버깅·재추론 가능. AUTO mode 정책 5(파괴적 작업 금지) 준수. **GT 노이즈 메타 룰** — 13건 broken 검수에서 7/13 (54%) GT 정정 → 평가 결과 부호가 뒤집혔음(-7.4%p → +3.9%p). VLM 평가에서 GT 노이즈는 결론 자체를 뒤집을 수 있음. 전수 GT 검수 없이 단편적 결론 금지.

2026-04-30 vlm/cherry-pick-pro | 작업: Round 2 액션 #2 — Pro cherry-pick 8건 검증 (drinking 4 + moving→eating_paste 4) → **Pro 25% (2/8), Flash 0/8**. drinking 4건 중 1건만 Pro 살림(2420abd8) → 시각 한계 가설 부분 검증, **3건은 모델 차이로도 못 풀음** (memory `feedback_vlm_visual_information_limit.md` 정공법: UX 통합/메타데이터/HITL). moving→eating_paste 4건 중 3건이 Pro도 동일 오답 → **prompt rule 9 over-trigger가 모델 공통 문제**. spec § 3-11 추가, README 갱신. | 참조: general#3 vlm#1,5,6 | 지킴: general#3 (Pro 25% 결과를 "Pro 별 효과 없음"으로 단정 X — A/B 카테고리별로 분리 분석 → A는 시각 한계 / B는 prompt 한계 별개 결론 도출), vlm#1 (Pro로 모델 단일 변수 검증 — 시스템 prompt/temperature/top_p/response_mime_type 동일 고정), vlm#5 (Pro의 conf 1.00 7/8 케이스 = 확신에 찬 오답 패턴 인지 → "근거 강제" 룰 도입 시 동일 confabulation 반복 위험 재확인. memory `feedback_vlm_confidence_abstain_limit.md` 적용 — abstain은 Pro에서도 무력), vlm#6 (`temperature=0.1, top_p=0.95, response_mime_type=application/json` 동일 fix — Flash와 비교 가능한 결정론 보장) | 놓침: -. | 재발: -. | 메모: **"모델 업그레이드 ROI 낮음" 검증** — Pro 단가 ~10x, 속도 ~10x 느림(avg 14.2s/clip) → 운영은 Flash 유지 + hard case는 별도 처리 분기 (UX 통합 / HITL). **A vs B 진단 분리의 가치** — "Pro도 못 풀음 = 다 시각 한계"로 묶어버리면 잘못된 처방. A는 픽셀 정보 없음 / B는 prompt 시그널 잘못 → **fix 위치가 완전히 다름** (UX vs prompt). cherry-pick 8건만으로도 이 분리 가능 (대규모 ablation 없이도 진단). **다음 액션 명확** — (1) drinking + eating_paste UI 통합(feeding) 검토, (2) rule 9 약화 또는 negative case 보강 실험, (3) Sonnet 4.6 추후 동일 8건 검증.

2026-04-30 vlm/feeding-merge-159 | 작업: inbox/0430 52건 import (8 클래스 GT 자동라벨 + 예외 2건 moving + 원제목 notes 보존) → v3.5 zero-shot 159건 재추론 → feeding 통합 (drinking ≡ eating_paste, raw 보존) 평가 레이어 적용. **raw 81.8% → feeding 통합 86.2% (+4.4%p)**. recovered 8 > broken 4 가드 통과 (107건 8/6보다 깨끗). 신규 52건 raw 86.5%. shedding 97% (n=29, 7배 늘었어도 유지) / eating_paste 100% (n=17) / drinking raw 50% → feeding 83%. drinking + eating_paste 묶음 정확도 93.1% (27/29). spec § 3-12 추가, README 갱신, memory에 feeding 통합 결정 검증 메모 추가. | 참조: general#1,2,3,4 vlm#1,5,6 python#11,15 | 지킴: general#3 (52건 추가 후 결과를 "+0.5%p 좋다"로 끝내지 않고 신규/기존 분리 + 클래스별 raw vs feeding 매트릭스로 진단), general#4 (자체 검수 후 사용자 보고 — feeding 통합 effect 증명을 drinking + eating_paste 묶음 93.1%로 직접 측정), general#1 (label_from_name 매핑 dry-run으로 0 unmapped 검증 후 실제 import — 어제 eating-paste prefix 룰 누락 패턴 회복), general#2 (import 스크립트 어제 패턴 재사용, label_from_name만 확장 — 새 헬퍼/추상화 추가 X), vlm#1 (Flash로 통일 — feeding 통합 효과 단독 측정), vlm#5 (feeding 통합은 prompt 룰 추가 X — 평가 레이어 매핑만 = confabulation 위험 0. raw 라벨 DB 보존으로 향후 모델 개선 시 분리 가능), vlm#6 (zero-shot 재추론도 동일 결정론 fix `temperature=0.1, top_p=0.95, response_mime_type=application/json` — Flash → Flash 비교라 모든 변수 고정), python#11 (`/Users/baek/petcam-lab/web/storage/poc-clips/2026-04-30/{uuid}.mp4` 절대 경로), python#15 (예외 2건 GT는 moving이지만 notes에 원제목 보존 — silent loss 방지) | 놓침: -. | 재발: -. | 메모: **"raw 보존 + UX 매핑" 패턴 검증** — memory `feedback_vlm_visual_information_limit.md` 정공법(b)을 평가 코드 5줄로 구현. drinking 시각 한계는 모델/prompt로 풀 수 없지만 UI에선 88.5% 정확도 노출 가능. 향후 Pro/Sonnet 측정에서 drinking 회복되면 raw 매핑 제거하면 끝. **신규 52건 86.5%** (기존 81.3% 대비 +5.2%p) — v3.5 prompt가 신규 데이터에 robust. 기존 평가셋이 약간 hard set이었음을 시사 (cherry-picked harder edge cases). **shedding 25개 추가에도 정확도 100% → 97%로 사실상 유지** — n=4 시점에 100%였으나 small-n bias 의심됐으나 7배 증가 후에도 robust. 신규 클래스 도입 비용·이익 평가의 좋은 case study. **moving → eating_paste 9건 over-trigger 패턴이 159건에서도 동일** — cherry-pick 가설(rule 9 over-trigger) 재확인. Round 3 rule 9 negative case 보강 또는 우선순위 조정 후보. **defecating 69%** — n=4 → 16으로 늘면서 새 실패 패턴 노출. small-n 정확도가 misleading일 수 있음. 적절한 모수 = 데이터 다양성을 노출시키는 트리거.

2026-05-02 nextjs/labeling-web | 작업: feature-r2 §3-7 라벨링 웹 — Supabase auth 게이트 layout + login + queue + clip 라벨 폼 + AppHeader `/labeling` 분기 + CORS + JSON URL endpoint (`/clips/{id}/file/url`) + 8 pytest 추가 (204 통과). | 참조: general#1,2 python#6 + React Rules of Hooks | 지킴: general#1 (Supabase JWT가 fetch Authorization 헤더로 전달되는지 + `<video src>`가 cross-origin Authorization 못 보내는 한계를 직접 확인 후 JSON URL 엔드포인트 패턴 도입), general#2 (라벨링 라우트 레이아웃은 `/labeling`만 영향, 기존 web/ 페이지 무영향. AppHeader는 `pathname.startsWith('/labeling') return null` 한 줄만 추가), python#6 (`labels_router`도 `Depends(get_supabase_client)` + `Depends(get_current_user_id)` — 테스트 override 가능). | 놓침: **Rules of Hooks 위반** — AppHeader.tsx 첫 수정 시 `if (pathname?.startsWith('/labeling')) return null;`을 useState 직후 + useEffect 이전에 배치. React는 모든 hook이 매 렌더 동일 순서로 호출돼야 해서 early return은 반드시 모든 hook(useEffect 포함) 호출 후. tsc는 통과(타입 무관)였고 npm run build 시 hooks rule lint에서 발견 가능했을 패턴. 자체 검수에서 hook 순서 의식하고 즉시 fix. | **재발: ⚠️ 1회째** — Next.js + 조건부 컴포넌트 렌더링 패턴 첫 도입. 다음 발생 시 donts/python.md 외 별도 `donts/web-react.md` 신설 검토 ("조건부 렌더 early return은 반드시 모든 hook 호출 후"). 메모리에 feedback 저장 보류 — 일반 React 상식이라 룰화 가치 미정. | 메모: **`<video src>` cross-origin Authorization 한계** — HTML5 video element는 fetch와 달리 Authorization 헤더 못 보냄. R2 signed URL은 자체가 auth token이라 video src로 직접 OK인데, 로컬 storage fallback은 별도 endpoint 필요. → JSON 엔드포인트 패턴(`{url, ttl_sec, type}`) 도입: 프론트가 fetch로 URL 받고 그걸 video src로. 동일 패턴이 thumbnail에도 적용. **CORSMiddleware allow_credentials + 명시 origin** — wildcard 못 씀. `LABELING_WEB_ORIGINS` env (콤마 분리)로 dev/prod 분기. **Auth 게이트는 client component layout으로 단순화** — @supabase/ssr 도입 안 하고 useEffect + onAuthStateChange + redirect 패턴. 라벨링 1명 운영 단계엔 충분. SSR 필요해지면 그때 마이그레이션. **`/clips/{id}/file/url` route 위치** — 기존 `/{clip_id}/thumbnail` route보다 먼저 등록(path segment 다르므로 충돌 없음). FastAPI route 등록 순서는 같은 segment 카운트 + path param에선 의미 있음. **사용자 가동 단계 분리** — Vercel 배포·Cloudflare DNS·라벨러 SQL 부트스트랩은 사용자 환경(브라우저 로그인·DNS provider 콘솔) 필요라 코드 작업 분리. spec § 3-7에 "사용자 작업" 명시.

2026-04-30 vlm/baseline-defense | 작업: Round 3 — v3.5 86.2% 잔존 오답(특히 moving → eating_paste 9건)을 prompt로 풀어보려 3가지 방향 시도, **3건 모두 baseline 대비 퇴행 → v3.5 production 확정**. (1) v3.6 rule 강화 + duration 메타 prior → 84.3% (-1.9%p, recovered 5 vs broken 10), (2) v3.7-B rule 약화 6 spots → 81.1% (-5.0%p, recovered 4 vs broken 12), (3) v4 clean-slate (legacy 무시 + 6 클래스 + 1641 chars) → 79.2% (-6.9%p, recovered 4 vs broken 15, GT=moving 64 중 16건이 feeding으로 false positive 폭발). 채택 가드(Δ > +2%p AND recovered > broken) 3개 모두 ❌. spec § 3-13 추가, README 갱신(✅ 완료 전환), memory feedback_vlm_rule_overcorrection.md에 3 시도 + 공통 메커니즘 + How to apply 누적, MEMORY.md 인덱스 갱신, prompt 백업 web/prompts/backups/system_base.v3.5.md/crested_gecko.v3.5.md. | 참조: general#3,6,8 vlm#1,5,6 | 지킴: general#3 (3개 시도 결과를 "퇴행"으로 단정 X — 5-카테고리 + confusion matrix + 클래스별 Δ로 mechanism 진단 → "v3.5 patch들이 false positive 방어 역할" 인과 도출), general#6 (자체 결론 신뢰 X — v3.6 한 번 실패 후 "v3.5 끝"으로 단정하지 않고 약화·clean-slate 방향까지 끝까지 검증한 후 baseline 보존 결론), general#8 (clean slate "단순함이 매력적"이라는 인상 신뢰 X — confusion matrix로 GT=moving→feeding 16건 폭발 직접 측정 후 채택 X), vlm#1 (Flash로 통일 — prompt 효과 단독 측정), vlm#5 (v3.6 duration 메타 prior 도입 시 confabulation 위험 인지 — "WEAK PRIOR / hint only" 명시했으나 모델은 강하게 받음. 룰 5(evidence-forcing) 패턴과 동형 확인), vlm#6 (temperature=0.1, top_p=0.95, response_mime_type=application/json 3개 추론 모두 동일 결정론) | 놓침: -. | **재발: 3회째 — Three-Strike Rule 충족.** v3.6(2026-04-30 1차) → v3.7-B(동일 세션) → v4(동일 세션). memory feedback_vlm_rule_overcorrection.md가 사실상 룰 역할(2회 시도 모두 퇴행 시 prompt 변경 stop). donts/vlm.md 정식 추가는 보류 — memory가 mechanism까지 담고 룰 5/6과 결이 같아 별도 룰로 분리하면 fragment 우려. 다음 baseline-defense 패턴 발생 시 정식 룰로 승격 검토. | 메모: **"v3.5 patch들은 trade-off 결과물 — burn-it-down 대상이 아님" 핵심 인사이트** — clean slate 단순함은 매력적이나 boundary가 흐려져 specific 클래스(feeding) 방향 false positive 폭발. patch들이 false positive 방어 역할을 하고 있었음. **"prompt 변경 자체가 attention 흔듦"** — 강·약 시그널 무관하게 prompt 수정이 모델 default를 흔드는 게 본질. donts/vlm.md 룰 5와 동형. **6 클래스 축소도 prompt 레벨에선 효과 없음** — 평가 레이어 매핑(93.1% 검증)과 prompt 레벨 매핑은 다른 메커니즘. 클래스 통합은 평가/UX에서만, prompt에선 raw 보존이 정공법. **"메타 prior는 명시 강도 무관하게 강하게 받음"** — v3.6 "WEAK PRIOR: long clips ≥ 30s lean moving"가 60s clip 6건을 "long=moving"에 휩쓸었음. 메타 prior 도입은 안 박는 게 안전. **"잔존 오답이 prompt로 안 풀리면 시각 한계 가정 → UX/HITL 정공법"** — 다음 액션은 (1) drinking + eating_paste UI 통합(feeding 묶음 이미 평가 레이어 93.1% 검증), (2) 메타데이터 보강(dish detection / before-after behavior), (3) HITL 저신뢰 케이스 운영자 검수 큐. **"채택 가드 Δ > +2%p AND recovered > broken"** 3회 연속 reject로 가드 신뢰성 확립 — recovered만 보고 채택했으면 v3.6/v3.7-B 둘 다 잘못 채택했을 수 있음 (v3.6 의도 클래스 +3.1%, v3.7-B v3.6 broken 6/10 회복). 부작용 합산 검증 필수.

2026-05-02 python/backfill-r2 | 작업: feature-r2 §3-4 실기 검증 갈음 — `scripts/backfill_motion_r2.py` (sync 단발, EncodeUploadWorker 로직 풀이) → motion 232건 (5 dry+real test + 227 full) 100% succ, 평균 압축 44.5% (914→410MB). NULL camera_id 88 자동 skip (PoC 업로드). spec §3-4 [x] + 88 NULL 결정 옵션 a/b/c 명시. dev server localhost:3001 가동(.env.local NEXT_PUBLIC_SUPABASE_ANON_KEY 적용). | 참조: general#1,2,3 python#7,11,15 vlm#- | 지킴: general#3 (10건 fail batch 발생 시 즉시 traceback 직독 → camera_id NULL 원인 진단 → SELECT 가드로 fix, 코드 추측 X), general#2 (`.not_.is_("camera_id", "null")` 필터 한 줄만 추가 — 새 헬퍼/skip 로직 분기 X), python#11 (스크립트 절대경로 `Path(__file__).resolve().parent.parent`), python#15 (`except Exception` 도 `noqa: BLE001` 명시 + traceback log + reason 누적 — silent X). 사용자 의도 재확인 (general#7 정신): "신규 모션 5건"보다 "기존 누적 R2 가시성"이 본질이라는 사용자 pivot 즉시 수용 → backfill로 §3-4 갈음. | 놓침: **첫 시도 SELECT에 NULL 가드 누락** — 376건 카운트만 확인하고 schema 가정대로 (camera_id NOT NULL FK) 작성 → 60건 진행 후 88건 fail noise 발생. 1차 시도 후 stop+fix+restart에 ~30초 비용. 데이터 가정 사전 확인(NULL/inf/타입 분포) 한 줄(`COUNT(*) FILTER (WHERE col IS NULL)`)로 회피 가능했음. | 재발: ⚠️ 1회째 — 새로운 스크립트 작성 시 데이터 분포 사전 확인 누락 패턴. 다음 발생 시 donts/python.md 후보로 ("DB SELECT 스크립트는 작성 전 NULL/타입 분포 1쿼리 확인"). | 메모: **"신규 5건 검증 vs 누적 backfill" 사용자 의사 차이** — spec wording은 narrow하지만 본질은 "R2 인프라 작동 확인". backfill 232/232 = 더 강한 evidence (인코딩 + 업로드 + DB 일관 232회 반복 통과). idle 246건 자동업로드도 부수 검증. **`r2_total=536` (motion 290 + idle 246)** vs spec 의도(motion만): idle 자동 업로드는 capture worker EncodeUploadWorker 정상 동작 fingerprint. **NULL camera_id 88건은 schema 가정 위반의 흔적** — Round 1 PoC web 업로드(`web/storage/poc-clips/`)가 camera_id 없이 INSERT. 라벨링 가치 평가 + attribution 결정 사용자 권한. **dev server 21h 노릇** — 사용자 기존 dev 안 건드리고 PORT=3001 별도 가동 (general#9 정신: 파괴 회피). NEXT_PUBLIC_SUPABASE_ANON_KEY 빈 채 빌드된 기존 3000은 라벨링 페이지 client throw 가능, 3001은 fresh 빌드라 정상.

2026-05-07 docker/fly-deploy | 작업: VLM 워커 fly.io 배포 (Dockerfile + fly.toml + secrets + /health) + E2E 검증 (clip 70093109 → moving 0.9 INSERT 75s). | 참조: general#1,2,11 python#11,15 | 지킴: general#1 (`docker run --env-file .env` 로컬 검증 후 fly deploy — 추측 X), general#2 (`backend/health.py` 신설 + `vlm_worker_main.amain()` 1군데 wiring + `tests/test_health.py` 4건. asyncio.gather 두 줄로 worker + health server 동거), general#11 (.env 키 `scripts/fly-set-secrets.sh` 7개 일괄 inject — `flyctl secrets import` stdin pipe, .env 자체는 image 비포함), python#11 (스크립트 절대경로 `Path(__file__).resolve().parent.parent`), python#15 (PromptNotFound 라이브 캡처 후 traceback 직독 → `.dockerignore` 원인 특정 — silent X). | 놓침: **`.dockerignore` 와 SOT 충돌 — `web/` 통째 제외 시 그 안의 `web/prompts/backups/` 누락**. 첫 deploy 후 워커가 `PromptNotFound` 던지며 무한 실패 (cycle 마다 transient=1). fix: `.dockerignore` 에 `web/*` + `!web/prompts/` (negation) + `Dockerfile` 의 builder/runtime stage 둘 다 `COPY web/prompts/` 추가. 학습 단계라 docker context 와 image 의 차이 + negation 패턴 직접 체험. | **재발: ⚠️ 1회째** — 디렉토리 통째 제외 후 안의 일부 파일이 코드 의존 경로로 read 되는 구조 인지 못 함. 다음 발생 시 donts/python.md 후보 (12번 옆): "디렉토리 통째 ignore 전 grep 으로 코드 의존 파일 없는지 확인. 또는 SOT 위치 자체를 분리 (web/ 안에 backend 의존 파일 두지 말 것)". memory `feedback_dockerignore_sot_conflict.md` 에 1회 기록. **재발 2회째면** spec follow-up "prompts SOT 위치 backend/ 이전" 즉시 진행. | 메모: **"학습 + production" 패턴 정확히 작동** — `.dockerignore` syntax 의 디렉토리 매칭 우선 룰을 직접 fail 로 체험 후 negation 패턴으로 교정. 이전엔 책으로만 알던 거. **`flyctl scale count 1` 수동 필요** — `[http_service]` 정의 시 fly 가 첫 deploy 에 머신 2개 띄우는 패턴 (regional redundancy). `min_machines_running = 1` 만으론 max 강제 안 됨. 베타 비용 절감 위해 명시 다운스케일. **`AUTH_MODE=dev` cosmetic 미스매치** — 워커는 AUTH_MODE 미사용 (HTTP API 없고 service_role 직접) 인데 `vlm_worker_main.bootstrap()` 의 logger.warning 이 dev 출력. fly.toml `[env]` 에 `AUTH_MODE='prod'` 명시 추가 follow-up 으로. 동작엔 영향 0. **prompts SOT 분리 follow-up** — `web/prompts/` 가 라벨링 웹과 워커 양쪽이 read 하는 구조. `.dockerignore` 사고가 또 나면 `backend/prompts/` 로 옮기고 web 이 read 하는 식이 더 안전. spec § 4 follow-up 에 기록.

2026-05-02 python/backfill-r2-null-cam | 작업: 1차 backfill 후 §3-4 후속 — NULL camera_id 88 (PoC web 업로드, `eval/v35/v3.5-zeroshot.jsonl` 평가셋과 1:1 매칭) 결정. 사용자 옵션 (b) "uploaded literal + 관리자/테스터 라벨 검수" 채택 → backfill 스크립트 SELECT 가드 제거 + R2 키 패턴 `clips/uploaded/{date}/{stem}_{id}.mp4` 로 분기. 88/88 succ 157s. 후속 `backend/routers/labels.py` LabelCreate에 `labeled_by` optional 추가 → owner override 권한 (관리자/테스터가 다른 라벨러 라벨 강제 수정/생성). 19 테스트 통과 (15 기존 + 4 신규: owner override / owner 타인 신규 / labeler 403 / self-explicit 통과). spec §3-6 권한 표 갱신. | 참조: general#1,2 python#15 | 지킴: general#1 (LabelCreate 변경 전 기존 routers/labels.py + tests/test_labels_api.py 풀읽기 → FakeSupabase upsert 동작 확인 후 테스트 작성, 추측 X), general#2 (LabelCreate 필드 1개 + handler 4줄 + 테스트 4개 = 최소 변경. 별도 endpoint 신설/RLS 정책 변경 X — 백엔드 단일 게이트로 권한 일원화), python#15 (`raise HTTPException(status_code=403, detail="...")` 명시 + 메시지에 의도 박음 — silent X) | 놓침: -. | 재발: -. | 메모: **"camera_seg = row['camera_id'] or 'uploaded'" 한 줄 분기** — Path operator(/)가 NoneType TypeError 내던 1차 사고 → SELECT 가드 대신 R2 키 namespace 분기로 처리. attribution 분리 + idempotent 재실행 + 라벨러 권한 분기 모두 한 분기로 해결. **owner-override 패턴 = `body.X != self_id and clip.owner != self_id → 403`** — RLS 정책 추가 없이 백엔드 한 곳에서 체크. supabase RLS 우회 service_role 사용 중이라 백엔드가 단일 권한 게이트. 라벨링 웹 → 백엔드 → DB 단일 흐름 유지 (라벨링 웹이 직접 Supabase 안 씀). 향후 라벨링 웹 직접 Supabase 접근 시 RLS 정책 추가 필요. **사용자 명시 의도 "관리자&테스터 라벨 확인/수정"** → "(ii) owner가 다른 라벨러 라벨도 강제 수정 - supabase 유저별 권한부여 필요" 인지 → 권한부여는 백엔드 분기로 충분(RLS는 부수 가드). 사용자 의도 정확히 짚음. **PoC 평가셋 159 = DB 88 (NULL cam) + 71 (cam capture)** — eval/v35/v3.5-zeroshot.jsonl과 cross-reference로 검증. v3.5 baseline 86.2%/85.5% 평가에 그대로 사용 가능. **테스트 케이스 설계** — owner-self / owner-other-labeler-update / owner-other-labeler-new / labeler-other-fail / labeler-self-explicit-pass = 5분기 표본화. 기존 테스트(자기 row UPSERT) 회귀 보존.

2026-05-07 nextjs/r2/supabase | 작업: 라벨링 웹 owner 검수 4 endpoint 를 Vercel→Supabase/R2 직결로 이전 (`feature-labeling-web-cloud`). `web/src/lib/{r2.ts:presignGet, clipPerms.ts}` 신규 + `app/api/clips/[id]/{route.ts:GET, file/url, labels, inference}/route.ts` 4개 + `labelingApi.ts` request() 분기 (`/api/` prefix → same-origin, else → BACKEND_URL). tsc 통과. 실기 검증: clip 3b0d9995 영상 + VLM shedding conf 1.00 + reasoning 표시. 커밋 c81b5b2. | 참조: general#1,2,3,7 | 지킴: general#1 (Edit 전에 Read 강제 — specs/README.md 한 번 안 읽고 Edit 시도 → "File has not been read" 에러 → Read 후 재시도. backend/clip_perms.py 풀읽기 후 TypeScript 미러로 작성 — 추측 X), general#2 (4 route + 2 helper + 1 분기 = 최소 변경. labelingApi.ts 함수 시그니처 무수정, path 만 `/api/...` 로 변경), general#3 (사용자 "백엔드 530인데 vercel 영상 보임?" 진단 단서를 `curl -I api.tera-ai.uk/health`(530) vs `petcam-vlm-worker.fly.dev/health`(200) 사실 확정 → labelingApi.ts BACKEND_URL 의존 4개 endpoint 라인 단위 추적), general#7 (web/ 폴더 구조 유지 — `lib/` 새 파일 2개 + `app/api/clips/[id]/{file/url,labels,inference}/route.ts` 신규. 폴더 명명/패턴 변경 0) | 놓침: `npm run build` 시도 → donts#9 (Claude Code 안에서 build 금지) hook 으로 차단. tsc --noEmit 만으로 진행한 게 정답이지만 자동 시도부터 함. | 재발: ⚠️ build 시도 1회째 — 다음 발생 시 정식 룰 후보 ("Next.js 작업 시 npm run build 자동 시도 금지, tsc --noEmit + Vercel 자동 빌드로 검증"). | 메모: **same-origin 분기 패턴** — `path.startsWith('/api/') ? path : ${BACKEND_URL}${path}` 한 줄로 단계적 이전 가능. 호출부 (`getClip`, `getMyLabels`, `getInference`, `getClipFileUrl`) 무수정, path 만 `/api/clips/...` 로 바꿔 자동 라우팅. **외부인=404 패턴** — backend `clip_perms.py` 의 owner/labeler/outsider 모델을 TypeScript 미러. 403 대신 404 통일로 ID enumeration 차단 (existence leak 방지). **service_role + auth.getUser(token)** — Supabase 클라이언트 service_role 키로 `auth.getUser(jwt)` 호출 → user 객체 반환. RLS 우회 라우트에서 토큰 정체성 확인 표준 패턴 (boto3 의 `presigned_url('get_object', ...)` 동치). **R2 GET signed URL** — `@aws-sdk/client-s3` `GetObjectCommand` + `getSignedUrl(client, cmd, {expiresIn: 3600})`. PutObjectCommand 와 같은 SDK 의 GET 버전. 1h TTL backend `DEFAULT_SIGNED_URL_TTL` 동치. **NextResponse 패턴** — discriminated union `{ ok: true, ... } | { ok: false, response: NextResponse }` 으로 helper 가 응답 직접 만들어 반환 → 호출부 한 줄로 early return. **검증: clip 3b0d9995 영상 + VLM shedding conf 1.00 + reasoning 표시** = fly.io VLM 워커 + Vercel 직결 endpoint 동시 가동 증거. macbook off 시나리오 라벨링 웹 owner 흐름 검증 완.

2026-05-08 cloudflare/dns-cutover/api | 작업: API 서버 fly.io production cutover (`feature-api-server-fly-deploy` Phase 3+4). `flyctl certs create api.tera-ai.uk` → Cloudflare DNS GUI 에서 Tunnel CNAME 삭제 → A `66.241.124.67` + AAAA `2a09:8280:1::1f:c84e:0` (DNS only, gray cloud) 추가 → ~30초 안에 Let's Encrypt E8 cert Issued (만료 2026-08-06) → `curl -I https://api.tera-ai.uk/health` HTTP/2 200 + `openssl s_client` cert subject 검증 → `flyctl certs check` 후 `Configured: true / DNS Validation: PASSED`. 사용자 맥북 cloudflared/uvicorn 이미 클린 (실행 X). Phase 4 = DEPLOYMENT.md / ARCHITECTURE.md / specs(README/next-session/feature/handoff)/handoff-prompts 일괄 갱신 — 옛 Cloudflare Named Tunnel 흐름은 Appendix(history) 로 보존. | 참조: general#1,9 + cloudflare DNS UI 사전 분석 | 지킴: general#1 (DNS 변경 직전 `dig +short api.tera-ai.uk A/AAAA` 로 Tunnel CNAME 가리키는지 확인 → cutover 후 즉시 fly.io IP 반환 검증. 추측 X), general#9 (Tunnel CNAME 삭제는 destructive — 사용자 GUI 에서 직접 삭제 + 스크린샷 확인하며 진행. CLI 자동화 시도 X). cert 발급 시간 expectation 1~3분 → 실제 ~30초로 빠르게 완료. cutover 직후 검증 chain (dig immediate → curl HTTP 301 → flyctl certs show Status=Issued → curl HTTPS 200 + openssl cert validation) 5단 직렬 진단으로 어느 단계에서도 fail 시 격리 가능했음. | 놓침: **Tunnel record 가 Cloudflare DNS UI 에서 "Type=CNAME (Tunnel)" cosmetic label 로 표시되는데 underlying 은 일반 CNAME 이라 직접 삭제 가능 — 처음엔 Zero Trust Tunnels 페이지에서 disable 해야 한다고 가정**. 사용자 스크린샷 받고 즉시 정정 ("CNAME 옆 Edit/Delete 메뉴 그대로 사용") → 사용자가 GUI 에서 한 번에 삭제 + 새 A/AAAA 추가 완료. cutover 진행에 영향 0 (1~2분 사용자 대기 비용만). | **재발: ⚠️ 1회째** — Cloudflare 의 "Zero Trust 에서 만든 record" 가 Dashboard DNS 에 표시되는 방식 인지 부족. 다음 발생 시 (a) Cloudflare Tunnel CNAME 은 일반 CNAME 이고 DNS 페이지에서 직접 삭제 가능, (b) Tunnel 자체는 "Configure → Public Hostname" 에서 별도 정리 (record 삭제 후), (c) Zero Trust Tunnels 가 dashboard-managed 면 Tunnel 객체와 DNS record 가 분리됨. memory `feedback_cloudflare_tunnel_dashboard_dns.md` 신규 작성 검토 — 다음 cutover/DNS 작업에 시간 절약. | 메모: **cert HTTP-01 challenge 실측 ~30초** — fly.io 매뉴얼 expectation "1~3분" 보다 훨씬 빠름. DNS propagation 이 즉시 (Cloudflare AS13335 fast resolver) + LE staging 대기 시간 없음 영향. Monitor 폴링 도구로 polling loop 짰는데 직접 `flyctl certs show` 한 번이 더 빠른 검증. **DNS only (gray cloud) 강제** — Cloudflare Proxy 는 LE HTTP-01 challenge path (`/.well-known/acme-challenge/`) 를 fly edge 로 안 보냄 (자체 캐시/규칙 적용). cert 발급 후 Proxy 켜면 다시 challenge 깨짐 → fly.io 권장 = DNS only 유지. **A 1개 + AAAA 1개 = fly.io edge anycast** — `66.241.124.67` (IPv4) / `2a09:8280:1::1f:c84e:0` (IPv6) 둘 다 region 무관 anycast. nrt 머신은 이 IP 의 라우팅 결과. **Cloudflare Tunnel 폐기 = `cloudflared` 서비스 종료 + Tunnel 객체 삭제 (Zero Trust → Tunnels)** 두 단계. 사용자 맥북에서 `brew services list` cloudflared "none" 이미 정리됨. Tunnel 객체 자체는 Zero Trust dashboard 에서 별도 정리 필요 (DNS record 삭제만으로는 객체 살아있음). 비용 0 이라 방치 OK. **Phase 4 docs 갱신 = 5분기 (3 docs + 4 specs + 1 handoff + 1 audit)** 일괄 처리 — DEPLOYMENT/ARCHITECTURE 는 ASCII 다이어그램 통째로 다시 그림 (옛 Tunnel chain → Cloudflare DNS → fly.io edge 직결), 옛 흐름은 Appendix 로 보존해 history grep 가능. spec README 표 1행 + next-session 헤더/시스템상태/Stage 라인 + flutter-cloud-handoff 백엔드 가동상태/footnote + handoff-prompts 옵션 A/B 제거. **롤백 비용 = DNS record 원복 + Tunnel 재가동 (사용자 맥북 `cloudflared service start`) ~5분** — fly.io 머신 자체는 살아있어 DNS 만 되돌리면 즉시 Tunnel 흐름 복원. fly.io 머신 죽이지 않는 한 안전한 cutover.

2026-05-08 docker/fly-deploy/api | 작업: API 서버 fly.io staging 가동 (`feature-api-server-fly-deploy` Phase 1+2). Phase 1 = endpoint 2개 (`/me/is_labeler` + `/clips/highlights`) commit `b458bb0` (pytest 247, 신규 8). Phase 2 = `Dockerfile.api` + `fly.api.toml` + `scripts/fly-set-secrets-api.sh` + `flyctl deploy` → `petcam-api.fly.dev/health` 200 (nrt, shared-cpu-1x 256MB, 165MB Depot push, 1대 scale-down). | 참조: general#1,2,9,11 python#1,11,15 | 지킴: general#1 (`docker run --env-file .env -p 8765:8000` 로컬 검증 → /health 200 + 86.79 MiB 메모리 측정 후 fly deploy. 추측 X), general#2 (워커 Dockerfile / fly.toml 패턴 그대로 미러 — 새 추상화/옵션 추가 X. 변경 핵심은 entrypoint + EXPOSE + COPY 범위만), general#9 (`flyctl scale count 1` 수동 다운스케일은 fly 의 first-deploy redundancy=2 동작 인지 후 명시 적용), general#11 (`scripts/fly-set-secrets-api.sh` 8개 SECRET_KEYS — `.env` 의 placeholder/누락 검증 + `flyctl secrets import` stdin pipe), python#1 (`uv add`/`uv remove` 로 `opencv-python` → `opencv-python-headless` 교체, lock 동기화), python#11 (스크립트 절대 경로 `Path(__file__)`), python#15 (HTTPException 메시지 명시 + raise; bare except 없음). DISTINCT ON 이 PostgREST 미지원이라 Python set merge 로 우회 결정 + 베타 트래픽 가정 명시. | 놓침: **`opencv-python` 직접 사용** — 첫 docker build 후 `cv2` import 시 `libxcb.so.1` not found (X11 deps 누락). python:3.12-slim 베이스에 GUI 시스템 라이브러리 없음. fix = `opencv-python-headless` 교체 (821MB → 771MB). 또: **secrets import 스크립트 stdin pipe 이슈** — `./scripts/fly-set-secrets-api.sh` 두 번 호출, 첫 번째 ✅ 두 번째 ❌, 그러나 `flyctl secrets list` 0건. 셸/zsh + Claude Code `! ` 환경에서 stdin pipe 가 빈 입력 전달되는 듯. 우회 = `grep -E '^(KEY1|KEY2|...)=' .env | flyctl secrets import -a petcam-api` 한 줄. flyctl 이 빈 stdin 도 silent 통과해 ✅ 보였음. | **재발: ⚠️ 1회째 — `opencv-python` headless 미스 + secrets stdin pipe 이슈 둘 다.** 다음 발생 시 (a) `donts/python.md` 후보 "headless 컨테이너 = `-headless` 변종 (cv2/PIL/matplotlib)", (b) `donts/python.md` 후보 "secrets import 같은 결정적 명령은 출력 verify (`Set N secrets`) 직접 확인 — exit 0 / ✅ 만 신뢰 X". 메모리 `feedback_dockerignore_sot_conflict.md` 옆에 `feedback_docker_opencv_headless.md` + `feedback_secrets_stdin_silent.md` 신규 작성 검토. | 메모: **fly.io 첫 deploy 패턴 — 워커와 동일** (`feature-vlm-worker-fly-deploy` 참조) → `[http_service]` 정의 시 머신 2대 띄움 → `flyctl scale count 1` 수동. `min_machines_running=1` 만으론 max 강제 안 됨. 베타 비용 절감 ($1.94/월 × 1대) 위해 명시 다운스케일. **DISTINCT ON 없는 supabase-py + PostgREST** — `behavior_labels` (human) 와 `behavior_logs` (vlm) 두 테이블에서 clip 당 1건 (human 우선) 반환을 Python set merge 로 우회. 베타 트래픽 가정 (< 5K clips) → in-memory dedup OK. IN-list 1000+ 시 RPC 로 이전 메모. **이미지 크기 차이** 로컬 build 771MB vs Depot remote build 165MB push — Depot 가 multi-stage 결과물에서 attestation manifest 등 빠진 단일 platform manifest 만 push. 첫 fly deploy 빠르게 끝남 (~3분). **메모리 86.79 MiB / 256MB VM (33%)** 로컬 측정. clips list/highlights 가 500+ row 반환 시 spike 가능 — 200MB 초과 시 `scale memory 512` 한 줄. **`AUTH_MODE=prod` 강제** — backend/auth.py dev 모드 우회 사고 차단 위해 `[env]` 평문 박음. secret 으로 안 박아도 운영 노출 무방 (cosmetic 분류 결정). **DNS cutover 는 별도 phase 분리** — staging 가동 검증 끝난 시점에 commit (b458bb0 + Phase 2) → DNS 전환 (Phase 3) 별도 commit. 인프라 롤백 시 endpoint 코드는 살림.

2026-06-02 meta/spec | 작업: `experiment-claude-subscription-rba.md` 신규 — 사용자가 가져온 "Claude 구독모델" 5개 아이디어를 중복 제거 후 evidence 스펙 검증용 저비용 트랙 스펙으로 구조화 + specs/README 표 갱신. | 참조: general#1,2,8 | 지킴: general#1 (SOT 직접 Read 후 작성 — `AI-VIDEO-ANALYSIS-STRATEGY.md` §10.1/§10.6, `experiment-event-segment-vlm.md`, `experiment-mac-mini-segmentvlm-worker.md`, evidence 스펙, `donts/vlm.md` 룰 5/6 실제 확인 후 인용. 기억으로 전략 단정 X), general#2 (5개 아이디어 그대로 dump 안 함 — 2/4/5 는 기존 Track B 스펙 중복으로 판정 후 참조 처리, 새 축 1개(analyzer 과금 모델)만 남김 → 중복 스펙 양산 회피), general#8 (외부 AI 출처 brainstorm = 초안 취급 → 비판적 검수: 모델 불일치 함정 + 재현성 함정 + $1.78 비용 정체 미규명 3개 짚음. 맹신 X) | 놓침: - | 재발: - | 메모: **핵심 교훈 — "또 다른 트랙" 요청은 새 스펙 만들기 전에 기존 트랙 목록과 dedupe 먼저.** 이번 5개 중 3개(컨택트시트=§4.7~4.8 / 모션키프레임=§4.3 / 하이브리드2단계=evidence+§1.2)가 이미 설계돼 있었음. dedupe 안 했으면 중복 스펙 + drift. donts 승격 후보 아님(general#8 이 이미 커버) — 1회 기록. **부가 — 검증 트랙 설계 시 "검증 모델 ≠ production 모델" 분리**: Claude(구독)로 검증해도 production 은 Gemini → 리포트에서 "방법론 검증"과 "모델 일반화"를 항상 분리해야 거짓 자신감 방지. **실행 레포 주의**: 이 스펙은 petcam-lab 에서 설계했지만 Claude CLI/Code batch 실행은 CLAUDE.md 자매레포 룰상 petcam-rba-worker 영역 → 실행 단계 전환 시 미러+sync 필요.

2026-06-07 vlm/hand-feeding-ood | 작업: rba-worker HITL 핸드오버 반영 — hand_feeding OOD 라벨 5곳 전파(C-1) + 라벨링 OOD UX(C-2) + v3.6 후보 프롬프트 + `build_system_prompt` 버전 격리로 v3.5 9-class production 보존(C-3) + GT 정정 6건 Supabase sync + 159건 오답 진단(비용0). 커밋 676dfd4. 회귀평가는 Gemini AQ. key 계정 플래그로 차단(크레딧 X). | 참조: general#1,2,3,8 vlm#1,5,6 python#11,15 | 지킴: general#1 (보고서 가정 vs 실제 레포 6곳 Read/DB 대조 후 진행 — build_system_prompt 가 `.md` 백업 read 구조 / 라벨링 MAIN·RAW 하드코딩 / action CHECK 부재 / labels.py Literal 다 확인, 외부 문서 코드 가정 맹신 X), general#3 (회귀평가 전건 실패를 "크레딧 소진" 단정 X → 단일 호출 + key 형식 진단 → AQ. prefix key 계정 플래그 확정), general#8 (핸드오버=초안 취급 — 정의 정밀화 4개 이미 v3.5 에 있어 제외, multi-event Phase B Out, 보고서 코드 예시 우리 구조와 안 맞아 재설계), vlm#5 (v3.6 OOD 룰은 evidence-forcing 아닌 시각 사실 "도구 보이나"라 confabulation 위험 낮음), vlm#6 (eval 결정론 fix 재사용) | 놓침: **prompts.py BEHAVIOR_CLASSES 에 hand_feeding 넣을 때 build_system_prompt→classes_block→v3.5 production 프롬프트 누출 위험을 처음 못 봄.** worker.py(131-132 호출) 추적 중 발견 → 버전 격리(`_VERSION_EXCLUDED_CLASSES`)로 차단. 라벨 enum 이 production 프롬프트 입력에 자동 전파되는 구조 인지 늦음. | 재발: - | 메모: **에러 메시지 액면 단정 위험** — 429 "credits depleted" 를 크레딧 소진으로 진단했다가 사용자 "key 바꿨다" 단서로 파보니 AQ. prefix key 계정 플래그(공개 노출 후 자동 플래그) 이슈. 에러 메시지 ≠ 근본 원인, 사용자 컨텍스트가 진짜 단서였음. **타 레포 핸드오버 코드 가정은 실제 대조 필수** — `baek-end` 경로 기준 보고서가 우리 실제와 6곳 달랐음 (특히 build_system_prompt 구조). **라벨 체계 ≠ VLM 출력 클래스** — web types(10, 라벨링용) vs backend prompts(production 9 / v3.6 10) 가 hand_feeding 도입으로 역할 분기, 단순 미러 가정 깨짐. **159건 진단으로 다음 전략 확정** — 그릇↔먹기 13건=시각한계(영상 추가 무의미, ROI/UX) / defecating 69%=데이터 부족(영상 추가 가치) / hand_feeding=v3.6 가 풀 듯(Gemini 가 도구 이미 인지) / VideoMAEv2 검증 과녁=그릇↔먹기 시간적 동작.

2026-06-08 vlm/eval-set | 작업: inbox/0608 신규 44건 evidence셋 R2(`clips/eval-0608/`)+camera_clips+behavior_logs GT 등록(`register_eval0608.py` dry-run→apply 멱등) + 평가셋 **159 동결→203 통합**(사용자 결정, `load_eval_set`=203 / `load_eval_set_0608`=44, 159=203−44 복원가능) + Claude v3.6 contact-sheet **blind 정성평가**(서브에이전트 6명, GT 미공개). raw 77.3% / hand_feeding OOD recall 12/14 / hard-negative→moving✅(과탐0). 커밋 예정. | 참조: general#1,2,3,8 vlm#1,5 python#11,15 | 지킴: general#1 (camera_clips/behavior_logs 스키마 + 159 카운트 + owner/pet 을 `_probe` 진단 스크립트로 DB 실조회 후 INSERT — 추측 X. camera_clips 가 정확히 159=평가셋 그 자체임을 발견), general#3 (159 동결 깨짐 위험을 `load_eval_set` 코드 추적으로 사전 발견 → 화이트리스트 가드 먼저 깐 뒤 203 전환 합의), general#8 (자기확증 회피 — GT 아는 내가 판정 X, GT 미공개 서브에이전트 6명 blind + `meta.json` Read 차단), vlm#5 (OOD 룰=시각사실 "도구 보이나"라 confabulation 낮음). | 놓침: **평가셋 "동결 vs 통합"을 등록 착수 전에 안 물어 Phase 1(159 동결 코드화) 했다가 사용자 "203 운영" 결정으로 일부 롤백.** 등록·평가 evidence셋 작업은 "평가셋 운영 방식"부터 확정했어야 왕복 0. | 재발: - | 메모: **hard negative 역설** — contact sheet 정성트랙은 미세신호 손실로 보수적(과탐0)이라 hard-negative 1건이 "쉽게 통과", 진짜 변별력은 Gemini 영상네이티브(과탐위험↑)에서만 나옴. **오답 10건 전부 정답→moving 과소 = 입력(contact sheet) 한계지 v3.6 결함 X** (같은 paste 도 close-up 95% vs 작은dish moving). 정량은 Gemini key 복구 후 `load_eval_set_0608` 로. **`r2_uploader.upload_clip` 는 put_object 단일=100MB+ 부적합 → boto3 `upload_file`(자동 멀티파트)로 우회** — 4K 영상 多, python donts 후보(1회라 메모만).

2026-06-08 vlm/eval-set/GT정정 | 작업: blind 평가가 발견한 라벨 오류 정정 — `2961` hand_feeding→eating_paste (파일명이 hand-feeding 이었으나 실제는 도구 없이 dish paste 핥기). 로컬 파일명 + R2 key(copy+delete) + camera_clips + behavior_logs + meta.json + blind.jsonl + REPORT 7곳 일괄 + 재집계(raw 34→35/44, OOD recall 12/14→12/13, GT 분포 paste9→10·hand_feeding14→13). | 참조: general#1,3 | 지킴: general#1 (정정 전 clip_id/r2_key/action DB 실조회로 대상 확정 — 추측 X), general#3 (GT 오류를 blind-AI 판정 + human(사용자) 합의로 확정 후 정정). | 재발: - | 메모: **★ blind 평가(GT 차단)가 라벨 QA 효과** — 서브에이전트가 GT 못 보니 "정답 끼워맞추기" 못 하고 픽셀 사실로만 판단 → 등록 라벨 오류를 잡아냄(2961: "도구 없음, dish paste 라 hand_feeding 아님" 명시). **신규 evidence셋 등록 후 blind 1회 = GT 검수 루틴 후보.** [[feedback_eval_set_freeze_no_cherrypick]] "2-human 불일치 GT-noise 정정"의 human+blind-AI 합의판. **R2 객체 rename = copy_object + delete_object** (S3 key 불변, server-side copy 라 로컬 재업로드 불필요).

2026-06-08 vlm/eval-203/v3.6.1 | 작업: Claude 트랙으로 203 적합 153건 blind 평가 → contact sheet 클래스별 적합도 맵(moving/hf 90%대 가능 · drinking/prey/paste 미세접촉 한계 · shedding/defecating/hiding/unseen 완전불가) + GT 오류 8건 정정(사람 도구급여 eating_prey/paste→hand_feeding, 누적 9건) + v3.6.1 OOD 룰 초안(급여 행위로 좁힘, 정성 11/11, 채택보류). | 참조: general#1,3,6,8 vlm#1,5 | 지킴: general#1(파일럿+8장 육안 교차검증 후에만 정정/룰설계 — 추측X), general#3(GT 오류를 blind 판정+휴먼 육안 교차로 확정 후 정정), general#6(같은모델 자기리뷰 회피 — blind 에이전트(GT 모름) 판정을 내가 GT 알고 교차검증), general#8(v3.6.1=초안 취급 + CLAUDE.md 버전격리(v3.6 무손상, 새 system_base.v3.6.1.md + prompt_version 분기) + 회귀없이 채택 절대보류), vlm#5(OOD 룰=시각사실 "도구가 음식 주나"). | 놓침: 사용자 "6x6+N회" 선택을 파일럿 없이 203 전수에 박을 뻔 — 파일럿(shedding5+defecating3)으로 6x6 무효 입증 → 5x6 유지(44 재사용) 비용 절감. | 재발: - | 메모: **파일럿 먼저 = 비용 폭발 방지** (효과 미검증 설정을 전수 전 소수로 검증 — defecating 0%/shedding 20% 확인 후 미세행동 50건 제외). **blind=라벨 QA 누적 9건** (GT 차단이 라벨 오류 역검출). **v3.6.1 정성 11/11(과발동 5수정+recall 6유지)이어도 채택 보류** — contact sheet≠production Gemini + 회귀 필수(v3.5 lock 6번 실패 교훈).

2026-06-10 vlm/frames-202/fable5 | 작업: Fable 5 첫 세션 — frames 202건 blind 재실행(서브에이전트 26명, v3.6.1 프롬프트·추출프레임 동일, **모델만 교체**) + 이전 run 정정GT 재채점 + paired 비교(`_score_frames_fable5.py`). 이전 81.7%(165/202, 79.7%+GT정정 4건) → Fable 5 **85.1%**(172/202, recovered 11/broken 4, McNemar p≈0.12 단독 유의 X). moving 93→**100%**(→unseen 오답 5건 전멸 = 체계적 개선), `036a650d`(이전 모델이 게코 혀를 시린지로 오판한 그 클립) drinking 정답 회복. defecating 19→31%·drinking 69% 동결 — 순간이벤트/벽응결수 한계는 모델로 안 풀림 재확인. | 참조: general#1,8,10 vlm#1,5 | 지킴: general#8(blind 유지 — 에이전트 meta.json 차단, GT는 채점기만 접근), general#10(26명 각자 배치 Read만, 파일 쓰기는 메인 단독 취합), vlm 모델불일치 함정(§6.1) — 이전 수치와 직접 비교 대신 "새 모델 baseline" 명기. | 놓침: - | 재발: - | 메모: **모델 업그레이드 효과는 양성이나 p≈0.12** — 입력표현(+21%p)만큼 크지 않고 단독 채택근거 부족. hand_feeding 이탈 1건(`53d52acb` gt=unseen, 손이 먹이컵 배치 — 게코 부재 급여행위) = OOD 룰 경계 케이스, 사람 영상 확인 대기. 이전 run 모델 미기록 교훈 → **이후 blind 리포트에 세션 모델명 기록**(이번: claude-fable-5).

2026-06-10 vlm/weak-model-levers/P1 | 작업: 약한모델 레버 스펙(`experiment-weak-model-levers.md`) 착수 — P1 Opus 4.8 + P1b Sonnet 4.6 frames 202 blind(각 26에이전트, model= 인자로 분기) + 4모델 비교 채점기(`_score_frames_models.py`, McNemar+합의분석). 결과 Fable 85.1 > Opus 81.2 > Sonnet 78.2 > (이전 81.7). **★격차 원천=Sonnet moving 93→76% IR창백패치 shedding 과탐 단일 실패모드** → 레버 B 타깃 확정. majority-vote 85.6%≈Fable단독=레버D 협소. | 참조: general#1,8,10 vlm#1,6 | 지킴: general#8(blind 4모델 동일 프로토콜, GT 채점기만), general#10(78에이전트 각자 Read만 메인 단독 취합), 스펙기반(3일+ 실험 _template 복사→스코프/완료조건 먼저→사용자확인 후 착수), vlm 모델불일치함정(Claude 정성≠Gemini production 명기). | 놓침: B06 Opus 에이전트 8건중 1건(`5cfe1d48`) JSON 누락 — 26배치×8건 취합 시 건수 assert 가 잡음(202 검증 실패→보충 1에이전트). **다음부터 에이전트 출력 즉시 건수 체크**. | 재발: -(frames 누락은 신규) | 메모: **약한모델 끌어올리기 = 격차의 실패모드부터 격리**. "전반적으로 약하다"가 아니라 단일 혼동(IR→shedding)에 집중 → 표적 룰 1개로 회수 가능 가설. recall↑인데 정확도↓(Sonnet shedding) = 과탐 정밀도손실, 클래스 recall만 보면 오판. model= 서브에이전트 인자로 동일 하네스 다모델 평가 패턴 확립(opus/sonnet/fable/haiku 교체 가능).

2026-06-10 vlm/weak-model-levers/P4 | 작업: 레버 C 캐스케이드 시뮬(`_sim_cascade.py`, 기존 jsonl 3개 오프라인 결합, 신규 인퍼런스 0). base=Sonnet→strong=Fable 라우팅 룰별 (정확도,에스컬레이션율). ★R1 "Sonnet=shedding만 escalate" 23%→격차 100% 회수(85.1=Fable동급). conf단독(R3) 같은회수에 53% 필요(2.3배), random 23%는 36%만 → 표적 +4.5%p. | 참조: general#1,3 vlm#6 | 지킴: general#3(추측 X — P1b 발견에서 라우팅 트리거 도출), vlm#6(conf 단독분기 무효를 대조군으로 재실증 — `confidence_abstain_limit`), 인퍼런스 0 설계(per-clip pred 가 이미 jsonl, 라우팅만 오프라인 재생=녹화 fixture 라우터 unit test). | 놓침: - | 재발: - | 메모: **단일 실패모드 → 단일 라우팅 트리거.** 약점이 한 클래스(shedding 과탐)에 집중하니 "그 클래스 예측만 escalate"가 conf/random 압도. 캐스케이드 설계는 "어디서 틀리나" 진단(P1b) 먼저 → 거기에 트리거 맞춤. conf 임계는 "어디 틀렸나"를 못 가리켜 비효율. Gemini production 청사진=Flash→shedding판정만 Pro. **오프라인 시뮬로 호출예산 정책을 인퍼런스 0에 검증** = 종량제 비용 정책 사전평가 패턴.

2026-06-10 vlm/weak-model-levers/P2 | 작업: 레버 A 입력표현 — 배변 제외 시 남은 ceiling 오답 진단(`_err_breakdown.py`: Fable 배변+물 제외 91.8%, 남은 14오답 중 12 →moving) → 가설 "결정적순간이 시간샘플에서 샘" 검증. 모션키프레임 N=20(균등10+모션피크10) 오답셋11+대조군9 재추출(`_p2_extract_keyframes.py`, ffmpeg+cv2 프레임차분)→Fable blind 재판정→채점(`_score_p2.py`). **recovered 1/11(shedding만)·broken 0/9 = +0.5%p 기준미달 → 가설 기각.** | 참조: general#1,3,4 vlm#5 python#11 | 지킴: vlm#5(의심되면 롤백/오버셀 금지 — +0.5%p 를 "효과 있음"으로 포장 안 하고 기각 명시), general#4(자체 채점+대조군 broken 측정 후 결론), general#3(진단 먼저 `_err_breakdown` → 그 다음 타깃 추출), python#11(절대경로 Path). | 놓침: "프레임 수 N↑ 단독" arm 미검증 — 모션셀렉터만 봄(공간한계라 효과 낮을 추정이나 미실측, 소표본 11). | 재발: - | 메모: **★ 자기 가설 기각도 결과**(좋은 실험=가설 죽일 수 있어야). 모션에너지 맹점: 프레임차분은 gross 움직임만, 혀 날름(저모션 미세접촉)은 못 짚어 오히려 몸이동 프레임 선택 → eating_paste/prey 0 회복. **"결정적 순간"이 항상 고모션 아님**(먹이타격=고모션 잡힘 / 핥기=저모션 못잡음). close-up 대조군 9/9 vs 원거리 오답 전멸 = 게이팅=공간해상도지 시간밀도 아님 → ceiling 미세접촉 오답을 시각한계 버킷으로 재분류(입력표현 레버 소진). 진단레이어 [[feedback_accuracy_diagnosis_layering]] 갱신: 입력표현 다음은 모델/영상네이티브, 프레임트릭 추가 무효.

2026-06-11 vlm/weak-model-levers/P3 | 작업: 레버 B 표적룰 — v3.6.2-draft(v3.6.1 + IR 야간 shedding 가드 rule8 caveat 1줄) 버전격리 생성(`prompt_version` 분기, v3.6.1 diff 1줄 무손상) → Sonnet shedding 예측 46건(오탐19+정탐27) v3.6.2 재판정 ablation(`_score_p3.py`). recovered 14/19(IR moving 오탐 전부)·broken 0/27(주간 허물 전부 유지). Sonnet 78.2→85.1% 투영(=Fable). full-202 배치준비(156)했으나 Gemini회귀 key-blocked로 승격 비-actionable→묶어 대기. | 참조: general#1,3,8 vlm#5 | 지킴: vlm#5(근거강제 회피 — "근거 적어라" 아니라 "IR창백패치=아티팩트, 주간peeling 예외"로 정의 좁히기, broken 0 부작용없음), general#8(blind 유지 GT 채점기만), CLAUDE.md 프롬프트 버전격리(v3.6.1 무손상 + 새 파일 + 분기 + DEFAULT 승격금지 회귀전), 토큰 스튜어드십(full-202 2.4M 비-actionable이라 보류 — 안 묻고 판단). | 놓침: v3.6.1 동시재실행 대조 안 함(stored 비교=세션간 변이 가능) — recovered 의 IR경계 정확일치로 변이 배제했으나 이상적이진 않음. blind=라벨QA 부산물(5a34267c·ce9bab20 GT-noise 후보) 즉시 정정 아니라 사람검증 대기로 보존. | 재발: - | 메모: **★ 단일 실패모드 → 단일 표적룰 1줄로 격차 전부 회수**(P1b 진단→P4 캐스케이드 검증→P3 룰 구현, 3단 일관). 약한모델 끌어올리기 가장 싼 레버=프롬프트(추론비용 동일, 캐스케이드 호출 0). vs P2(천장 입력표현) 기각 대비 = ROI 바닥에 있음 재확인. **표적룰 형식=정의 좁히기**(proximity≠eating 가드와 동형), 근거강제와 구분. **비-actionable 확인에 토큰 안 태움**(full-202 돌려도 Gemini 게이트 막혀 승격 불가→key 복구 시 일괄).

2026-06-12 vlm/pivot/gemini-retirement | 작업: key 복구 확인(같은 AQ. 키 200 — Google 전역이슈 해제) → DB GT 4건 sync(`_sync_gt_corrections_0609_r2.py` dry-run→apply→멱등검증) → 4버전×202 정량회귀 병렬 가동 → **63%(145건)에서 사용자 피벗("Gemini 포기")으로 중단**, paired 클로징 박제(`experiments/gemini-final-partial/` — IR가드 Gemini −2.3%p=표적룰 모델특이성 실증, OOD 16/16) → AskUserQuestion 4갈래(런 처리/몽타주 목적/production/claude-video) → 계획서 `experiment-claude-montage-v2.md` 작성. | 참조: general#1,3,9 vlm#3 | 지킴: general#1(AQ.=401 단정 안 하고 ping 재검증 — 메모리 "해결됨" 정정), general#3(sync는 기존 0609 스크립트 패턴 재사용+dry-run 먼저), 파괴적작업 회피(kill 전 진행률 실측 제시+사용자 결정으로 실행, 부분데이터 보존), 스펙규칙(계획서=템플릿 구조, 사용자 승인 후 착수 명시). | 놓침: 4런 병렬 시작을 피벗 신호 전에 했음 — "key 복구=즉시 1순위 실행"이 spec 락인이라 정당했지만, 비용 발생 작업은 가동 직전 사용자 핑 한 번이 더 안전했을 수 있음(~$2.3 소비, 단 클로징 기록으로 회수됨). | 재발: - | 메모: **피벗 시 자산 분류가 핵심** — 죽는 것(Gemini 게이트)과 사는 것(dataset/blind 프로토콜/paired 분석/캐스케이드 설계) 구분해 계획서에 승계 명시. 중단된 실험도 paired면 박제 가치 있음(같은 클립 같은 순서 = 145건 교차비교 유효). 메모리는 "점-시간 관찰" — 외부서비스 차단 메모리는 만료 가능성 항상 열어둘 것.

2026-06-13 vlm/v4.0-prompt + 평가셋정리 | 작업: 사용자 대화로 v4.0 신설 — ①defecating/basking/hiding 폐기(`_VERSION_EXCLUDED_CLASSES["v4.0"]`) ②drinking "물 보임(시각증거)"→"몸 고정+반복 핥기(행동패턴), 물 안 보여도 OK" 재정의 + "1회 충분" 폐기 + 장소로 paste 구분 + 손흔들림 caveat + 부분가림/클로즈업 보강. 평가셋 202→185(defecating16 폐기 + cf698b78 평가부적합). 입력기준 긴변 min(원본,1080) no-upscale 확정. | 참조: general#1,7 vlm#5 CLAUDE.md(프롬프트 버전격리) research-testing | 지킴: 버전격리(v3.6.1·v3.5 무손상, 새 파일 system_base/crested_gecko.v4.0.md + 분기, 15테스트 통과), 확정은 사람영상(drinking 오답 4건 풀해상도 직접 확인 → 6a24c2e6=입력해상도/3369d723=부분가림/cf698b78=GT부적합 원인 분리), 데이터보존(_excluded 이동 삭제X + manifest.bak-202), GT-noise 후보 5a34267c·ce9bab20(shedding 의심) _excluded 보존→복원가능, vlm#5(보강 도입 전 "효과 있나" 영상으로 먼저 검증 — 사용자 요구), no-upscale(가짜픽셀 토큰낭비 회피). | 놓침: DB(behavior_logs) defecating/cf698b78 미반영 — Gemini 퇴역으로 DB회귀 무의미·Claude 트랙 manifest SOT라 의도적 보류(필요시 sync). | 재발: - | 메모: **★ 입력 해상도가 미세접촉(혀)의 1차 병목** — 같은 클립이 몽타주 셀(346~522px)에선 혀 점으로 소실/원본 1080px에선 drinking 명확(6a24c2e6·3369d723 실증). 프롬프트(v4.0)는 절반, 입력(풀해상도 frames)이 절반. 회귀는 frames-10@1080 필수(몽타주로 하면 v4.0 정의 무력). 혀 근본해결=ROI crop(YOLO). 웹캠 스펙=해상도×비트레이트×거리 3변수(미세접촉은 압축률에도 민감 — 1Mbps 실측은 디테일 아슬). **약한모델 레버 P2 "게이팅=공간해상도" 결론이 drinking 케이스로 재확인.**

2026-06-15 vlm/평가셋187+B1-quality_tag | 작업: A매듭(CLAUDE.md 평가정책=전체187기본/185 paired회귀 동결 + next-session 186→187 동기화) + Opus vs Sonnet 186 측정(Opus 88.7%>Sonnet 85.5% +3.2%p, decision `Opus 우위`, TEST-SHEET/REPORT/INDEX) + B1 manifest quality_tag 층화(`_add_quality_tag.py`/`_score_by_quality.py` — drinking 17 전수 + 오답 16 육안 visual, cam-motion 71 heuristic, untagged 99). | 참조: general#1,2,6 research-testing eval-set-freeze-no-cherrypick | 지킴: cherry-pick 회피(제거 아닌 **층화 태그**=합법적 대안), 확정은 육안(27건 프레임 직접 Read), blind 채점기 결정론(`_score_by_quality.py`), research-testing(Opus측정 시험지+보고서+INDEX 등록), 토큰 스튜어드십(전수 태깅 83건은 가치 대비 비용 커서 보류·사용자 직접으로 미룸). | 놓침: ② untagged 오답만 골라 태깅 → **selection bias**(closeup 38%·untagged 100% = 순환논리) 발생. 발견 즉시 정직 보고 + "quality 정확도는 전수 태깅 drinking 17만 신뢰" 한정 + REPORT §7-3 경고 박제로 수습. | 재발: - | 메모: **★ 클래스별 quality 민감도 다름**(drinking=sensitive 입력으로 풀림 / eating_prey=invariant closeup도 실패=더 깊은 시각한계 → 비-VLM). **selection bias = 새 함정**(Three-Strike 1/3 — 메모리 `selection-bias-error-only-tagging` + research-testing.md "해석 함정" 섹션 추가, 함정 주의보라 1회에도 룰화). R2 clip_id=UUID라 원본 파일명("10615") grep 안 잡힘(메모리 `r2-clip-id-uuid-mismatch`). 모델격차도 hard 샘플 집중=캐스케이드 근거.

2026-06-16 vlm/cascade-opus-sim(C) | 작업: 캐스케이드 시뮬 base=Sonnet→strong=Opus(186 v4.0, 인퍼런스0, `_sim_cascade_opus.py` 신규=기존 `_sim_cascade.py` P4/202 보존). 표적 클래스 라우팅(R1 shedding/R2 vuln)=random 동률→기각(격차 6건이 5클래스 분산), conf<0.7 16%호출=ceiling 100%회수(오답 conf 중앙 0.70<정답 0.88), eating_prey 22%·drinking 20%만 Opus 회수→비-VLM 필수(B2 스코프 확정). TEST-SHEET+REPORT+INDEX 등록. | 참조: general#1,7 research-testing + 메모리(vlm-confidence-abstain) | 지킴: research-testing(시험지 pre-reg→실행→보고서 decision→INDEX, 인퍼런스0이라 간단히), 합격기준 사후변경 없음(conf는 대조군이었으나 표적 능가 — D1 게이트 불변 적용+정직 보고로 분리), 기존구조 보존(general#7, 새 스크립트 파일 + `_sim_cascade.py` 무손상), 채점 SOT 재사용(`_score_opus_sonnet.py` 로더 동일=id join 무결). | 놓침: -. | 재발: -. | 메모: **★ 캐스케이드 라우팅 신호는 모델/프롬프트 버전에 따라 뒤집힌다** — P4(202 v3.6.1)=클래스>>conf(단일 실패모드 IR과탐, 고신뢰 오답이라 conf 못잡음) / C(186 v4.0)=conf>클래스=random(잔여오답이 저신뢰 모호). **conf 라우팅은 calibration 좋은 모델(Sonnet v4.0)에서만 유효** — `donts/vlm.md` "confidence-abstain 무용"(Gemini Flash)과 표면 긴장이나 (a)모델특이성 (b)abstain≠escalation 으로 구분(REPORT §5). **eating_prey 모델불변=비-VLM 확정**(Opus도 →moving 천장 = drinking과 동일 버킷, 더 심각).

2026-06-16(2) vlm/conf심화+B2-prey+학습 | 작업: conf 캐스케이드 심화 진단(calibration + 실측 가격 r=1.67 → 절감 30% 천장, temperature0 재측정=Sonnet만+인증 부재로 보류) + B2 RBA spec eating_prey 통합(§4.5/§6.5 drinking 동형) + 영상분류 학습노트(`docs/learning/video-classification-learning.md`). | 참조: general#1,2,7 research-testing | 지킴: general#1(기억단정 회피 — 가격=claude-api 스킬 확인·인증=Bash 실측, 추측 0), 사용자 아이디어 비판검토(계층줌인/몽타주 둘 다 이미 탐색됨 정직보고 + ROI crop 발전방향 제시), general#7(spec 5곳 정밀편집·기존구조 보존), research-testing(C/conf REPORT decision+INDEX), 토큰 스튜어드십(conf 재측정 인증 부재 확인 후 무리 진행 안 함). | 놓침: -. | 재발: -. | 메모: **학습레포 정공법 = 코드 실측 후 강의**(worker.py 영상직접·prompts.py DEFAULT v3.5 직접 읽고 가르침, 추측 금지). **사용자 아이디어가 이미 탐색된 결론에 독립 도달 = 이해 깊이 신호**(몽타주 셀↓→개별프레임 / 계층줌인→공간해상도 병목). 정지프레임 VLM 천장 넘는 길 = ROI crop(국소확대) / 영상네이티브 2개뿐. 메모리 `cascade-routing-signal-model-version-dependent` 신설.

2026-06-16(3) vlm/roi-crop-center(C1) | 작업: 계층줌인+ROI crop spec 풀기획(`experiment-hierarchical-zoom-roi-crop.md`) → C1 PoC. center ROI crop(원본 min(짧은변,1080) 정사각, 시간밀도 통제=crop만 변수, `_extract_frames_clip.py --roi-crop` 신규) 급여3종 56건 전수 paired vs 적응형@1080, Sonnet v4.0 blind(Agent 7+1명) → `_score_roi_crop.py` → **decision close**(Δ+0.0%p·순0·4K급10건 순0). | 참조: general#1,2,7 research-testing selection-bias vlm#6 | 지킴: research-testing(pre-reg 게이트 숫자고정→측정→REPORT decision→INDEX), 게이트 조정=인퍼런스 전 데이터발견(prey 4K급1/22→'prey≥2' 비현실)으로 **결과확인 전+사유기록**이라 룰 준수, 전수 56건 paired(selection bias 회피), blind(crop_{gt} 폴더명=GT노출 발견→셔플 익명 재구성), 게이트=Sonnet끼리만(Agent model:sonnet 고정, prompt-model-specificity), 기존구조 보존(extract_adaptive 무손상 옵션추가), 사용자 prey 스위트스팟 가설을 데이터로 반증. | 놓침: crop비율 0.5가 저해상 과소crop(328px) — 1건테스트로 발견·수정(인퍼런스 전이라 무해)했으나 spec 작성 때 해상도분포 먼저 봤으면 게이트 설계가 더 정확했을 것. | 재발: -. | 메모: **★ frame-side 입력 레버 완전 종료**(V1 다운스케일 천장 + ROI확대 천장 = 정지프레임 VLM 4레버 다 천장 → RBA 비-VLM 유일). **crop은 원본에서**(적응형@1080 결과물 crop=업스케일 무효=측정 무의미할뻔, 1건테스트가 잡음). **측정 전 데이터특성 확인이 게이트 설계 좌우**(prey 해상도 분포). **변동이 가설예측과 반대분포면 noise**(저해상서만 변동·고해상 0 = 공간해상도 가설 반증). 메모리 `roi-crop-close-frame-side-terminal` 신설.

2026-06-17 기타/RBA통합설계+eval-0617 | 작업: 자매 레포 2개(gecko-vision-gate·petcam-nightly-reporter) 아키텍처 설계(`specs/architecture.md` 각 신규+git init) + SOT `petcam-ai-pipeline.md §11` 통합 토폴로지 + eval-0617 10건 평가셋 등록(`register_eval0617.py`, manifest 197). | 참조: general#1,2,7,9,11 + research-testing | 지킴: general#1(register_eval0608 패턴·DB컬럼·`_extract_frames_clip` 클립소스 다 Read 확인 후 작성, 추측 0), general#2(tera-ai 내 파일만 selective add, market 변동 안 건드림), general#7(SOT §1~10 무손상 §11 append·기존구조 보존), general#11(R2/Supabase 키 .env), **외부쓰기(R2+camera_clips+behavior_logs INSERT) dry-run 검증→사용자 승인→--apply**, 사용자 아이디어 비판검토+더나은대안(Gate 게이팅 폐기·폴링>webhook·야간 분할). | 놓침: Edit old_string 첫시도 2회 실패(다이어그램 컬럼 정렬·partial Read는 full 아님) — 사소, 짧은 unique 앵커/재Read로 즉시 회복. | 재발: -. | 메모: **외부 쓰기(R2+DB)는 dry-run→승인→apply가 정석**(register 스크립트 패턴). **설계 큰 결정은 추측 말고 사용자와 대화로 확정**(Gate 정체성·트리거·분할=AskUserQuestion+대화 → 추측 설계 방지). **partial view Read는 Edit 권한 안 줌**(truncated/limit Read 후 해당 줄 재Read 필요). 메모리 `rba-pipeline-sister-repos` 신설.

2026-06-18 기타/펌웨어계약+dataset송부v4.0+wrap | 작업: nightly indexer R2 layout 설계(B방식=camera_clips.started_at 쿼리 확정, object store는 시간조회 약함) → 펌웨어 R2 clip 등록 계약 핸드오프(`docs/handoff-prompts/camera-firmware-clip-contract.md`, started_at=녹화시각, 회신대기) + dataset-203 전문가 송부 패키지 v4.0 갱신(README 전면재작성·`prompt_v4.0.md` 신규·analyze.py 적응형+7class) + DB GT sync 4건 유령항목 정리(실측=이미완료). | 참조: general#1,2,7,11 + research-testing | 지킴: general#1(R2 구조·camera_clips 스키마·capture key·build_system_prompt 출력 다 실측 후 설명/작성 — 교육도 코드 까서, 추측 0), general#11(service_role 키 펌웨어 임베드 금지 계약 명시), general#2(README/analyze.py 최소범위·기존 gemini/score 골격 보존), general#7(CLAUDE.md 자매레포 제목 보존+append·prompt 파일 분리), 메모리 verify(DB sync 유령 실측으로 헛수고 차단). | 놓침: "DB GT sync 4건 지금가능"을 옛 메모리 믿고 추천→까보니 이미완료(즉시 정정). next-session partial view라 Edit 거부 1회(재Read 회복). | 재발: △ recalled 메모리 stale 믿음 — 메모리 verify로 즉시 수습. | 메모: **★ object store는 시간 인덱스 아님**(R2 prefix listing 대신 DB started_at 쿼리=nightly indexer B방식). **효율=싸게+정확히**(started_at backfill 오염=빠른 오답, 정합성 계약 먼저). **SOT 함수 직접 실행해 박제**(build_system_prompt 출력→prompt_v4.0.md, 손조립 금지·species 하이픈 함정). **recalled 메모리는 스냅샷 verify**(팔로업 유령=닫을 때 즉시 갱신 안 해 누적). 메모리 3개 신설.

2026-06-19 기타/맥미니-Claude-러너 셋업 계획 | 작업: 맥미니 주기 Claude 워커 **개념 교육**(폴링 워커 패턴·맥미니=클라이언트 NAT→폴링강제·스케줄링 launchd/cron·Claude CLI headless·Slack webhook·walking-skeleton) + **계획 정렬**(`specs/feature-mac-mini-scheduled-claude-runner.md` 신규 + README/next-session 갱신). 코드 0, 공부+합의 단계. | 참조: general#1,9,11 | 지킴: general#9(작업 시작 `git fetch && status` 원격 확인=멀티머신 룰, 파괴적 작업 0), general#11(비밀값 `SUPABASE_*`/`SLACK_WEBHOOK_URL` .env+커밋금지를 스모크 완료조건에 박음), 사용자 아이디어 비판검토+보강(슬랙 전송 주체 claude→스크립트 분리·만들기 슬랙부터 거꾸로·스모크 cron 등 더 나은 대안 제시). | 놓침: -. | 재발: △ **partial view Read→Edit 거부 06-17·18·19 3연속** — 06-18 메모 알고도 next-session 첫 Edit 거부, 재Read 회복. | 메모: **개념 교육 세션도 spec+next-session 박제**(코드 0이어도 핵심결정 6개 휘발 방지 — 06-18 wrap 교훈 연장). **맥미니=클라이언트 멘탈모델이 폴링 강제 근거**. **★ partial-view-Read-then-Edit-거부 Three-Strike 도달** — 큰 파일은 Edit 직전 해당 줄 좁게 재Read를 기본 동작화(`rba-pipeline-sister-repos` 메모 행동강화).

2026-06-20 기타/맥미니워커 Phase0 스모크+cron→launchd+wrap | 작업: mac-runner 독립 레포(참조 스켈레톤=공유라이브러리 아님) Phase 0 walking-skeleton 스모크 맥북 6/6 검증(supabase 핑+`claude -p`+slack webhook, launchd 무인반복) + **cron→launchd 발견**(claude 구독 keychain=GUI 세션 필요, cron 데몬=세션 밖→`Not logged in`) + install-launchd.sh(머신별 plist) + GitHub push(`S-Soo100/petcam-mac-runner`) + petcam-lab 문서정리 + /wrap. | 참조: general#1,3,9,11 python#11,15 | 지킴: **general#3(진단 없는 수정 금지 — cron claude 실패를 `env -i` 재현→Not logged in→keychain 원인 격리 후 수정, 추측으로 안 고침)**, general#1(setup-token/`CLAUDE_CODE_OAUTH_TOKEN` 기억단정 말고 claude-code-guide+공식문서 확인), general#11(.env 가드 `git diff --cached grep .env` 매 커밋 + 스모크 완료조건에 박음), python#11(`load_dotenv` 절대경로 `Path(__file__)` cwd 함정 방어), python#15(특정 예외 httpx.HTTPError/subprocess.SubprocessError), 외부액션(GitHub repo+push) 승인 후, 사용자 아이디어 비판검토(통째 압축→독립레포+git·setup-token→launchd 선택 존중). | 놓침: -. | 재발: **○ partial-view Read→Edit 거부 4회째**(next-session 초반 truncated Read 후 Edit 거부→좁게 재Read 회복). 06-19 "Edit 직전 재Read 기본동작화" 메모 무색(이번은 초반 partial 후 한참 끼어 상태만료 성격). **Three-Strike 초과 → donts general 정식 승격 검토 필요.** | 메모: **★ cron vs launchd keychain 세션**(구독 claude headless=launchd 필수, 모든 맥미니 워커 공통, 메모리 `cron-launchd-keychain`). **walking skeleton이 본진(맥미니) 전 함정 격리**(맥북 cron 테스트로 keychain 문제 조기 발견 — 사용자 "맥북서 먼저" 발상 적중). **참조 스켈레톤≠공유 라이브러리**(워커 복붙, 얇은공통+두꺼운차이). **외부 자동화 인증 = keychain(세션 종속) vs env 토큰 trade-off**.

2026-06-20(2) 기타/맥미니워커 Phase0 맥미니검증+PATH버그+재부팅+한도 | 작업: 맥미니 본체 Phase0 검증 — clone(PRIVATE→gh auth)+uv sync+.env+smoke ✅ → install-launchd.sh launchd 등록 시 ❌claude(셸은 ✅) → 진단: 로그 `No such file 'claude'`=FileNotFoundError, uv(~/.local/bin)≠claude(/opt/homebrew/bin) 다른 bin인데 plist PATH에 uv만 → `command -v claude`도 잡아 PATH추가+가드 수정·push(c3f04e2) → FileVault가 자동로그인 차단(macOS 의도) → 보안 trade-off(service_role 평문 노출) 설명 후 사용자 FileVault off 선택 → 재부팅+자동로그인 생존 ✅(23:01) → 첫1회만✅ 이후❌ 반복 → 셸 직접호출 "session limit resets 1:10am"=구독 한도 초과(코드무죄), 1:12 리셋 복귀로 확증 → Phase0 완료로 스모크 bootout. | 참조: general#1,3,9,11 + systematic-debugging | 지킴: general#3(launchd ❌claude를 로그·셸재현으로 PATH vs keychain 격리 후 수정, 추측 0), general#1(`which uv claude` 실측으로 경로 확정·기억단정 회피), general#9(FileVault/자동로그인 보안 trade-off 사용자 설명 후 선택·강제변경 없음), 회귀0 검증(같은 bin이면 기존 동일 출력 dry-run), 멀티머신(push 전 fetch·맥미니 active 확인). | 놓침: -. | 재발: -. | 메모: **★ launchd PATH는 머신별 설치경로 차이를 잡아야**(맥북 uv+claude 둘다 brew=우연통과 / 맥미니 분리=깨짐. `command -v` 런타임 탐지 정석, 메모리 cron-launchd-keychain PATH함정 정정). **★ claude 구독 한도=워커+본인 공유**(5분 288회 폴링이 본인작업까지 막음, 모든 맥미니 워커 공통 — Phase1 한도분리 필수, 메모리 claude-subscription-quota-shared 신설). **Phase0 역할=인프라 검증이지 운영 아님**(검증 끝나면 멈추는 게 올바른 종료). **FileVault ON→자동로그인 원천차단=macOS 의도**(무인워커 GUI세션 전제와 충돌).

2026-06-30 운영/R2+DB 클립 삭제(p4cam-27b1f486 6/25~26) | 작업: dev 카메라 p4cam-27b1f486 의 KST 6/25~26 클립 일괄 삭제 — R2 658객체(mp4 328+jpg 330, 2.42GB) + `motion_clips` 454행. 삭제 전 진단(camera_clips=0·motion_clips만, KST 날짜별 집계, r2_key 프리픽스/NULL 검증) → dry-run 가드(윈도우 658 불일치시 abort)→`--apply`(에러0) → DB는 RETURNING count=454 → 검증(R2 윈도우 0·DB 0·주변 06-23/24/30 = 14객체/7행 무손상). | 참조: general#1,3,9,11 + 메모리 object-store-time-index | 지킴: **삭제 전 타겟 실측 후 진행**(r2_uploader·DB스키마 Read, 추측 0), **object-store-time-index 트랩 적용**(R2 LastModified 아닌 DB started_at KST 기준 특정 — 파일명 UTC `20260625-1721`이 KST 6/26 02:21임을 구분, 파일명 패턴매칭 회피), 파괴적 작업 승인(AskUserQuestion 으로 R2-only vs R2+DB 범위 확인 후 진행), dry-run→가드→apply 패턴(register 스크립트와 동형, 외부쓰기 정석), FK 안전 확인(motion_clips 인바운드 FK 0), 검증(양쪽 0 + 주변 무손상), 스크래치 스크립트 정리. | 놓침: -. | 재발: -. | 메모: **★ DB "업로드됨" 기록이 R2 실재를 보장하지 않음** — 삭제 전 DB는 454행 전부 r2_key 보유라 기록했으나 R2 실물 영상은 328개(126행이 이미 사라진 객체 가리키는 drift, 06-25엔 영상없는 썸네일 2개도). **삭제는 R2-listing 윈도우 기준이 DB-key 기준보다 견고**(유령참조 자동 무시 + orphan 객체 포착). **파일명=UTC, 운영시각=KST** 항상 분리해 사고(object-store-time-index 연장).

2026-07-07 라벨링/dataset-203 GT를 behavior_labels 백필(웹 큐 미완료 오표시 수정) | 작업: label.tera-ai.uk 큐가 이미 GT완료한 203셋을 "라벨필요"로 오표시 → 진단: 큐 완료판정은 **behavior_labels**(labels.py:264-296)로만 하는데 203 GT는 과거경로 **behavior_logs source='human'**(235)에 있고 behavior_labels엔 23건뿐 = GT 이중저장소 split → manifest.csv(203 SOT) gt를 owner 명의 behavior_labels로 백필(`scripts/backfill_gt_labels.py` dry-run→검증→--apply 196건, 기존1 skip) → 큐 213→17. | 참조: general#1,3,7,11 python#11,15 + research-testing(면제=데이터마이그레이션) | 지킴: **general#3(진단 없는 수정 금지 — 큐 로직·스키마·labelingApi Read로 "behavior_labels가 SOT" 격리 후 수정, 추측 0)**, general#1(스키마 list_tables 실측·enum ActionType Read 확인 후 gt 검증), 백필 전 3중검증(gt⊆enum·clip_id⊆camera_clips FK·기존 라벨 안 덮어씀 ignore_duplicates), **eval 격리 확인**(grep로 behavior_labels 소비처=웹뿐, eval은 behavior_logs human 읽음→무손상), dry-run→apply(register 스크립트 동형), 멱등(upsert on_conflict), python#11(load_dotenv 절대경로). | 놓침: -. | 재발: -. | 메모: **★ GT 이중저장소** — 라벨링 웹(behavior_labels) vs eval 로더(behavior_logs source=human)가 서로 다른 GT 테이블을 SOT로 봄. 한쪽 정정이 다른쪽에 전파 안 됨(drift 위험). 이번 백필은 웹만 동기화(eval 무손상). 남은 큐 17=v3.5 VLM import(defecating 폐기클래스 잔재, GT아님). 메모리 gt-label-store-split 신설.

2026-07-08 라벨링/큐 카드 개선(썸네일+VLM판정 태그) | 작업: 라벨 대기 큐 카드에 (1) 썸네일 표시 — labels.py queue enrich 로 각 clip r2_key(.mp4)→.jpg 치환 키의 presigned GET URL 을 응답에 포함(요청 1회), 프론트 ClipCard `<img loading=lazy onError→"영상" fallback>` (2) VLM 판정 태그 — behavior_logs source=vlm 최신 action 을 clip_id IN 배치 단일쿼리 join(N+1 회피)→`🔍{action}` Badge(info tone). ClipRow 파생필드(thumb_url·vlm_action) 추가. 테스트 3건 신규(vlm 최신1건+human무시·thumb .jpg치환·R2미설정 None), 회귀 labels 36 + 스모크 102 pass, `web` tsc 0. | 참조: general#1,2,3,7 python#4,15 | 지킴: **general#1(thumbnail_r2_key 전부 NULL·r2_key 형태·R2 실물 .jpg 존재를 head_object 로 실측 후 구현 — "같은경로 .jpg 있다" 사용자 주장 검증, 추측 0)**, general#3(기존 `/thumbnail/url` 엔드포인트가 왜 무용지물인지 진단=thumbnail_r2_key NULL → r2_key 파생 채택), general#2(최소변경 — 기존 queue 로직/seek pagination/권한스코프 안 건드리고 enrich 만 추가), general#7(폴더구조/네이밍/인터페이스 보존, 새 route 안 만듦), python#15(bare except 회피 — R2NotConfigured/ClientError/BotoCoreError 특정), best-effort 설계(enrich 실패해도 큐 목록 반환·thumb↔vlm 대칭). | 놓침: -. | 재발: -. | 메모: **★ thumbnail_r2_key 컬럼 NULL이어도 R2엔 clip 옆 같은이름 .jpg 실재** — encode_upload_worker(`thumbnails/` prefix)와 다른 업로드 경로라 컬럼 대신 r2_key .mp4→.jpg 치환이 실제 SOT. 컬럼 신뢰 말고 R2 실물 head_object 확인. **큐 파생필드 = best-effort enrich**(썸네일/VLM 태그는 부가정보 — presign·join 실패가 성공한 큐 목록을 죽이지 않게 둘 다 삼킴).

2026-07-08(2) VLM/프롬프트 v4.1 shedding IR가드 구현+회귀 → decision reject | 작업: nightly 흰모프 shedding 오탐 억제용 v4.1(근거 "창백색 대비"→"물리적 분리/벗겨짐" 이동 + IR가드, 버전격리 신규 3파일) 구현 → 시험지 pre-reg 동결(Set-FP 32 + Set-REG 185, 2파트 게이트) → blind 중립 스테이징 → Workflow 32배치 인퍼런스 → **오탐이 adaptive@1080서 재현 안 됨(v4.0·v4.1 64/64 moving)** → nightly 입력=랩 extract_adaptive 소스대조 identical → 원인=temperature 비결정성 → reject(프롬프트層 아님, v4.1 파일 보존·승격X). | 참조: research-testing(시험지+보고서) · CLAUDE.md 룰1-4(버전격리) · general#1,3 · donts/vlm#5(evidence-forcing),#6(temperature) | 지킴: **버전격리(v4.0 3파일 무손상 diff검증·v4.1 신규+_VERSION_EXCLUDED_CLASSES)**, **general#3(진단먼저 — DB로 shedding 2집단 분해=오탐32 production vs 진짜29 업로드, 추측 0)**, 시험지 실행전 동결·합격기준 사후 안 바꿈, **근본원인 격리(입력 소스대조로 배제→temperature 특정, "모델한계" 단정 회피)**, blind 무결성(중립 스테이징+grep로 gt/실험명 누출0 검증), **harness 버그 투명보고(오염 수치 인용 안 함·Part B 무효 명시)**. | 놓침: **★ harness 버그 — 공유 tasks.json index를 에이전트가 오독→배치 스크램블(5/32). 채점 전 배치별 sid 일치 검증 게이트 없어 늦게 발견.** ★ 멀티세션 — 미커밋 작업을 공유 인덱스에 staged 상태로 두는 동안 다른 세션 hard reset 에 소실(대화서 복구). | 재발: -. | 메모: **★ 워커 오탐은 프롬프트 새로 파기 전 "같은 입력+프롬프트로 재현되는지"부터**(64/64 미재현=노이즈이지 프롬프트 실패 아님, 메모리 nightly-shedding-fp-is-nondeterminism). **★ blind Workflow=배치별 개별파일**(공유 index 위임 금지, 메모리 blind-eval-workflow-per-batch-file). **★ 멀티세션 = 커밋+즉시 push 로 미커밋 작업 보호**(한 시점 한 세션 규칙). 메모리 2개 신설.

2026-07-08(3) 라벨링/큐·내라벨 썸네일+VLM태그+필터(프론트+백엔드) + ⚠️멀티세션 파괴적git 사고 | 작업: 라벨 대기 큐/내라벨 3개 개선 — 썸네일(API thumbnail/url 일원화·r2_key .jpg 파생) + VLM 판정태그(🔍action/미분석, 목표4) + 서버사이드 필터(queue 5축·mine 4축·filter-options). 세션중 원칙 3전환(프론트+백→"backend는 API개발자 요청서"→"API개발자 위임 backend 직접"). 최종 풀스택 feat/labeling-filters(4eea96b·21c1cf3·fd16c62), labels41+스모크107 통과·프론트 tsc0. | 참조: general#1,2,3,9 python#15 + 멀티세션룰 | 지킴: general#1(labels.py/clips.py/DB camera_id=uuid·source분포/R2 .jpg head_object 다 실측후 구현), general#2(기존 큐로직·pagination·권한 위에 필터 AND additive), general#9(사고후 극도신중 — north-star.md 미커밋 안건드리고 backend파일만 add·파괴적git 금지·커밋조기push), python#15(behavior_logs/camera 특정except), 브랜치+PR 협업전환. | 놓침: **★★ dirty tree(다른세션 v4.1 미커밋)에서 revert --no-commit→꼬임→revert --abort→다른세션 미커밋 소실위기.** fsck lost-found 복구가능 확인+사용자 v4.1 별도커밋(8d89030)로 무사. | 재발: **○ 멀티세션 공유트리 파괴적git 사고(최초·실증)** → 사용자 CLAUDE.md 멀티세션룰 즉시 룰化(worktree격리/커밋조기/dangerous-guard hook, 7fa737c·4f916c7). | 메모: **★ 공유워킹트리 2세션=파괴적git(revert/reset/clean/abort)이 상대 미커밋 소실** — 2번째세션 worktree격리 필수. **원칙은 소유권 따라 유연전환**(프론트만↔풀스택, API소유자 결정 존중). **thumbnail_r2_key NULL이어도 r2_key .jpg 실재**(head_object, encode_worker thumbnails/ prefix 아님). **다른테이블 필드 필터=clip_id 역쿼리 IN**(vlm_action=behavior_logs).

2026-07-13 라벨링/대화형 튜토리얼 풀스택 구현(마이그+게이트+API+UI, 12 Phase) | 작업: 신규 라벨러 온보딩 튜토리얼 — 설계문서 구현. writing-plans 계획서(실물 9파일 Read 후)→사용자 승인(구현범위/DB적용 AskUserQuestion)→Phase0 마이그(4테이블 RLS service_role·불변트리거·RPC5, apply_migration 적용·검증 client_policies=0/RLS4/funcs6)→lib(compareTutorialAnswers·decideTutorialAccess 순수함수+테스트)→게이트(requireProductionLabelingAccess·loadClipWithPerms 403 tutorial_required)→API(읽기 정답미노출 분기·쓰기 idempotency)→클라이언트→공유컴포넌트 추출(_labeling-forms, 상세페이지 동작보존)→튜토리얼 UI 상태머신→내비게이트+팀진행→문서. web 91 tests(+31)·next build 통과, Phase별 커밋. | 참조: general#1,2,3,7,9,11 + writing-plans(Critical GATE2) + test-driven-development + systematic-debugging + 멀티세션룰 | 지킴: **writing-plans(Critical 4+파일 계획서 먼저·실물 코드 근거·사용자 승인 후 착수)**, **general#1(labelingV2 validator·clipPerms 게이트·v2 라우트·migration 패턴·Badge tones 다 Read 후 구현, 추측 0)**, TDD(비교·게이트 순수함수 test-first, gt/vlm 라우트 idempotency·no-leak·no-behavior_labels 테스트로 provenance 잠금), **정답 조기노출 방지(stage 분기 서버강제 + gt 테스트가 reference/feedback 부재 assert)**, general#2/7(상세페이지 통째복제 안 함·공유컴포넌트 추출·저장API 분리·기존 v2 흐름 보존), **멀티세션(동시세션 경고 상태 → worktree 대신 Phase별 조기커밋+내 파일만 add로 방어)**, 리팩토링 동작보존(saveLabel 기본값=기존 문구·next build 검증). | 놓침: **★ cwd 오염 오진 — 문서 조사 `cd repo루트`가 persistent 셸 cwd를 바꿔, 이후 `npx vitest`가 web 밖에서 돌아 8파일 module-resolution/server-only 실패 → 순간 "코드 회귀"로 오인.** 즉시 진단(에러 전부 `@/` alias·server-only=환경) → cwd 원인 규명 → web/서 재실행 91 통과. | 재발: -. | 메모: **★ Bash 도구 cwd는 세션 지속** — 중간에 `cd otherdir && ...` 한 번이 이후 모든 상대 cwd 명령을 오염(vitest는 web/ 설정·`@/` alias 필요). 프로젝트 하위 명령은 절대경로 cd를 매번 붙이거나 `git -C`처럼 경로 명시. **테스트 급락=코드회귀 단정 전 실행환경(cwd/config) 먼저 의심**(에러가 assertion 아닌 import/resolution이면 환경). **★ apply_migration은 BEGIN/COMMIT 없이 본문만**(툴이 트랜잭션 자체관리, 파일엔 psql용 BEGIN/COMMIT 보존·툴 호출엔 제거). **★ 동시세션+대작업=Phase별 조기커밋이 worktree격리의 실용 대안**(내 파일만 targeted add로 상대 미커밋 안 건드림).

2026-07-13(2) 라벨링/튜토리얼 하드닝 7항목(후속 마이그·게이트·레거시 잠금·테스트) | 작업: 튜토리얼 seed/activation 전 하드닝 — (H1)이미 적용된 마이그레이션 미수정, 후속 `_hardening.sql`로 active/archived lesson 정답 불변 트리거 + seed/activation draft-only·완전성 검사 CREATE OR REPLACE → apply_migration + DB 롤백 DO블록 검증(update/delete/activate-nondraft/seed-active 4차단, 데이터 0잔류). (H2)Router Review verifyRouterReviewer에 tutorial 게이트 + 내비 미완료 labeler work 숨김. (H3)reset/waive 대상 labeler 검증 404·team-progress 전 .error 502. (H4)무인증 `/api/label`(호출처0·PoC잔재) owner-only 잠금. (H5)acknowledge·media·owner route 테스트. (H6)seed/activation 템플릿(실UUID 0). 검증 web 115·tsc·next build·Python 334. | 참조: general#1,2,3,9,11 + security-review + test-driven-development | 지킴: **이미 production 적용된 마이그레이션 미수정→후속 마이그레이션(원본 불변·재현성)**, **DB 트리거를 롤백 DO블록(execute_sql, RAISE로 전량 롤백)으로 실측 — 데이터 미잔류하며 4차단 확인**, **security(무인증 behavior_logs write hole 발견→owner-only+회귀테스트로 재오픈 방지)**, general#1(라우트 게이트·verifyRouterReviewer·isBehaviorClass·labelers SOT 실측 후 구현), general#2(최소변경 — 기존 게이트 위에 tutorial gate additive), 회귀 테스트로 계약 고정(라우트별). | 놓침: -. | 재발: -. | 메모: **★ 이미 적용된 마이그레이션은 후속 마이그레이션으로만 수정**(원본 파일 편집 금지, DDL은 CREATE OR REPLACE/DROP+CREATE로 누적). **★ DB 트리거/제약 검증=롤백 DO블록**(BEGIN…EXCEPTION로 각 차단 catch→플래그, 끝에 RAISE로 전량 롤백=production에 테스트 데이터 미잔류하며 실동작 확인). **★ 무인증 레거시 endpoint는 삭제보다 owner-only+회귀테스트**(재오픈 방지 계약이 테스트로 남음). **★ 게이트는 진입점마다**(loadClipWithPerms만으론 리스트 API verifyRouterReviewer가 새어 — 인증 헬퍼 전수 점검 필요).

2026-07-13(3) 라벨링/튜토리얼 2차 하드닝(OLD 기준 트리거·오류 은닉·오류 테스트) | 작업: 1차 하드닝 리뷰 반영 — (R1)이미 적용된 `_hardening.sql` 미수정, 2차 후속 `_hardening_2.sql`로 불변 트리거 판단을 COALESCE(NEW,OLD)→**OLD.tutorial_set_id 기준**으로(active lesson→draft set 이동 우회 차단) + 차단필드 5→10(set/position/title/objective/tip 추가). apply + DB 롤백검증(move/position/title/reference/delete 5차단). (R2)verifyRouterReviewer labelers 오류 `supabase error:<msg>` 누출→databaseUnavailable(일반 502)로, 게이트 테스트 5(403/통과/owner bypass/오류 502). (R3)team-progress 4쿼리 각 오류 일반 502 테스트. 검증 web 125·tsc·build·Python 334. | 참조: general#1,3,9 + security-review | 지킴: **적용된 마이그레이션 미수정→2차 후속(누적 원칙 재확인)**, **OLD 기준 판단(NEW면 소속 변경으로 우회 — 트리거 판단 기준을 '현재 소속'으로)**, 롤백 DO블록 재사용(우회 3종 각각 catch·전량 롤백), **security(내부 Supabase 메시지 은닉 databaseUnavailable 일관 적용, 진입점 전수)**, 회귀 테스트로 오류경로까지 고정. | 놓침: -. | 재발: **○ cwd 오염 2회째 — pytest를 repo루트서 실행→cwd 루트 잔류→다음 vitest가 루트서 돌아 web/vitest.config.ts(server-only alias·@별칭) 못 타 server-only throw.** 이번엔 도구로그 `node_modules/server-only/index.js` 경로 단서로 즉시 cwd 진단(코드무죄), web서 재실행 통과. | 메모: **★ 불변/권한 트리거 판단 기준=행이 '현재 속한' 부모(OLD)**(NEW 기준이면 부모 재지정으로 우회 — active→draft 이동이 대표 사례). **★ cwd 함정 재발방지=web 명령(vitest/tsc/next)은 항상 `cd /abs/web &&` 프리픽스** 또는 `--root` 명시(pytest·docs 조사가 루트 cwd 남김). 테스트 급락+에러가 import/resolution/server-only면 코드 아닌 실행 cwd 먼저. **★ 내부 오류메시지 은닉은 databaseUnavailable로 일원화**(라우트마다 `supabase error:` 직접 쓰지 말 것 — 진입점 전수 점검).

2026-07-13(4) 라벨링 기준 GT·튜토리얼 UX 하드닝(중립선택·공통 issue 검증·조건부 form·owner 보정 RPC·seed preflight) | 작업: 설계문서(`2026-07-13-labeling-reference-ux-hardening-design.md`) 구현 — ①`collectGroundTruthIssues` client/server 공유 순수함수(9규칙, GroundTruthValidationError issues[]) TDD, ②폼 프리셀렉트 제거+조건부 숨김+인라인 오류+scroll(explicitlySelected 추적), ③튜토리얼 absent activity subjective 비교+lesson 문구, ④revision 테이블+`fn_revise_clip_labeling_session`(service_role, clip_id+reviewed_by로 세션 서버결정, initial_gt 불변, behavior_labels mirror) 후속 마이그+correction API/UI, ⑤seed RPC 3차 하드닝 position별 의미 preflight. apply_migration 2종+rollback probe(원자성·initial_gt 불변·reviewer/reason/mismatch 차단, 데이터 0잔류). web 155 tests·tsc·Python 334. | 참조: general#1,2,3,6,9,11 + test-driven-development + security-review + 적용마이그=후속원칙 | 지킴: **TDD(collectGroundTruthIssues 9규칙·allowedTargets·diff·absent비교·reference fixture·revise route 401/403/404/502 test-first)**, **general#1(labelingV2/gt route/vlm route/clipPerms/labelingAccess/behavior_labels 스키마·tutorial 마이그 패턴 다 Read 후 구현, RLS/grant 패턴 미러)**, **security(owner-only requireOwner·body revised_by/session_id 불신 서버결정·DB오류 databaseUnavailable 일반502·RLS ON 정책0·anon/auth execute revoke, advisor 확인=신규는 의도된 INFO뿐)**, **적용 마이그레이션 미수정→후속(`_hardening_3`)**, **rollback probe(DO블록 RAISE 전량롤백, 실데이터 0잔류로 원자성·불변 실측)**, general#2(공유 GroundTruthForm 확장·기존 v2 흐름 보존·최소변경), 스키마 가정 라이브DB 선검증 후 apply, DB 적용 전 AskUserQuestion. | 놓침: **★ cwd 오염 3회째 — 문서 grep `grep`가 셸 cwd를 루트로 옮겨, 직후 `npx tsc`가 루트서 돌아 "This is not the tsc command"(typescript 미설치).** 즉시 cwd 원인 규명→`cd web` 재실행 통과. Codex CLI도 모델버전(`gpt-5.6-sol`) 이슈로 죽어 교차검수는 Gemini 폴백. | 재발: **● cwd 오염 3회째 → donts 정식 룰 승격**(general#12 신설). | 메모: **★★ [승격] Bash cwd는 세션 지속 — web 명령(vitest/tsc/next)은 항상 `cd /abs/web &&` 프리픽스, pytest/grep 등 루트 명령과 섞지 말 것. 테스트/컴파일 급락+에러가 import/resolution/"not the tsc command"/server-only면 코드회귀 아닌 cwd 먼저.** **★ apply_migration은 BEGIN/COMMIT 없이 본문만**(파일엔 psql용 보존). **★ owner 보정 RPC는 세션을 clip_id+reviewed_by로 서버가 결정**(body의 session/clip/revised_by 불신 = §10). **★ 교차검수 Codex 죽으면 Gemini 폴백**(둘 다 비-Claude=자기리뷰 회피 충족, gemini는 파일 inline 필요). **★ npm run build는 레포 훅 차단(리소스 경합)→tsc --noEmit로 타입만, 실빌드는 사용자 터미널.**

2026-07-13(5) 라벨링 하드닝 후속 리뷰 반영(seed NULL-safe·position3 강화·revision append-only 트리거·revise clip 오류처리·확인 스냅샷) | 작업: 4차 구현 사용자 리뷰 8항목 — ①`_hardening_4.sql` 후속: seed preflight를 `IF (조건) IS NOT TRUE`로(JSON 키 누락 NULL이 검사 통과하던 구멍 봉인), ②position 3을 `target != tool`→`primary drinking + target ∈ 물집합`까지(moving+wheel 오답 reference 차단), ③`_labeling_session_revisions_append_only.sql` 후속: `fn_block_session_revision_mutation` 트리거로 service_role 포함 UPDATE/DELETE/TRUNCATE 차단(0A000, search_path='' 고정), ④revise route camera_clips 조회 `.error` 검사(DB오류 502·clip없음 404·무효duration 502, 60초 fallback 제거), ⑤CorrectionPanel 확인단계 진입 후 GT/VLM/사유 변경 시 useEffect로 확인 무효화+스냅샷만 저장, VLM review client 검증(validateVlmReview), ⑥TS 미러 evaluateTutorialReferenceSemantics position3 동기화, ⑦seed 템플릿 lesson4 feedback context_tags 해설, ⑧root .gitignore node_modules/. apply_migration 2종(+searchpath 재적용)+rollback probe(append-only UPDATE/DELETE 차단·누락키→22023·moving✗tool✗drinking+water✓, 데이터 0잔류)+advisor(신규 WARN searchpath 해소, revision INFO는 의도). web 160·tsc·Python 334. | 참조: general#1,2,3,6,9,12 + test-driven-development + security-review + 적용마이그=후속원칙 | 지킴: **적용 마이그레이션 미수정→후속(`_hardening_4`·`_append_only`)**, **NULL 3치논리 방어(IF NOT (NULL)=NULL 통과 구멍→IS NOT TRUE)**, **append-only는 REVOKE만으론 불충분(service_role GRANT ALL)→트리거로 역할무관 강제**, **TDD(revise clip 502/404/무효duration·position3 drinking/moving/tool test-first, web 155→160)**, general#12(web 명령 전부 `cd web` 프리픽스·pytest만 루트, cwd 안 섞음), **security(revise clip .error 누출 없이 502·advisor 신규 WARN까지 해소)**, TS/SQL 미러 동시 갱신(SOT 일치), 설계 §8.2 문서도 코드와 동기화. | 놓침: -. | 재발: -. | 메모: **★ SQL 의미검사는 `IF (양성조건) IS NOT TRUE`가 기본**(`IF NOT (…)`는 키 누락 NULL을 통과시킴 — JSONB `->>`가 없는 키에 NULL 반환하는 게 함정). **★ append-only 계약은 트리거로만 성립**(REVOKE는 service_role GRANT를 못 막음, 트리거는 session_replication_role=replica 슈퍼유저 우회 제외 전 역할 발동). **★ 트리거 append-only는 참조 FK의 ON DELETE CASCADE와 상호작용**(부모 hard-delete가 자식 CASCADE삭제→트리거 차단→부모 삭제 실패, 감사행 영구보존 의도면 OK지만 문서화 필수). **★ 조회 fallback 기본값(duration=60)은 검증 우회**(잘못된 값으로 통과할 수 있어 — .error/부재/무효를 명확히 끊기). **★ 확인 후 편집 무효화=useEffect([입력])로 확인상태 리셋 + 저장은 스냅샷만**(확인요약과 저장 payload 불일치 방지). **★ 트리거 함수도 search_path 고정**(RAISE만 해도 린터 WARN — `SET search_path=''`).

2026-07-13(6) DELETE /api/clips/[id] 삭제 원자성 하드닝(부분삭제 방지) | 작업: R2·behavior_labels/logs 를 먼저 지우고 camera_clips 를 마지막에 지우던 순서가, tutorial lesson RESTRICT 나 revision append-only 트리거가 마지막 삭제를 막으면 영상·라벨은 이미 사라진 "부분 삭제"를 냄. ①프로덕션 FK/ON DELETE 규칙 선확인(9개 참조: tutorial_lessons만 RESTRICT, 나머지 behavior_labels/logs·sessions·revisions·router_* 전부 CASCADE) → ②DB 삭제를 R2보다 먼저, ③명시 behavior_labels/logs delete 제거하고 **camera_clips 단일 원자 DELETE**(삭제 대상 child 전부 CASCADE라 RPC 불필요), ④거부 시 409(FK RESTRICT 23503·append-only 0A000 각각 도메인 메시지, 그 외 DB오류는 내부 은닉 500), ⑤DB 성공 후에만 R2 best-effort, ⑥client deleteClip 이 서버 409 메시지 표시(하드코딩 문구 폐기), ⑦DELETE route 테스트 7(revision 409+R2미삭제·tutorial 409+R2미삭제·기타DB 은닉+R2미삭제·정상 DB→R2·R2 best-effort·401·403), ⑧labelingTutorial.ts 103행 SOT 주석 _hardening_3→_hardening_4. DB 롤백 probe로 실측: revision 붙인 clip 의 `DELETE FROM camera_clips` 가 0A000 차단 + **behavior_labels 무변(부분삭제 0)**, 전량 롤백. web 167·tsc·Python 334·git diff --check clean. | 참조: general#1,2,3,9 + security-review + test-driven-development | 지킴: **FK/ON DELETE 규칙 라이브DB 선확인 후 설계**(추측 아님), **부분삭제 방지=DB 먼저+단일 원자 DELETE**(외부 시스템 R2 는 DB 성공 후에만), **거부는 롤백=아무것도 안 지워짐 실측**(probe로 labels 무변 확인), **내부 DB 메시지 은닉**(예전 `labErr.message` 노출→일반 메시지, code 로만 분기), **append-only 트리거+CASCADE 상호작용을 삭제 계약에 활용**(revision 있으면 clip 삭제 불가=감사 영구보존), TDD(4필수+3보강 test-first), general#2(순서 반전·명시삭제 제거=최소 정합 변경). | 놓침: -. | 재발: -. | 메모: **★ 다중 스토어 삭제는 DB(원자) 먼저, 외부 시스템(R2 등) 나중**(외부를 먼저 지우면 DB 거부 시 부분삭제 복구불가 — 방향은 "추적 가능한 orphan"쪽으로). **★ child FK 가 전부 CASCADE면 부모 단일 DELETE=원자**(명시 다중삭제·RPC 불필요, RESTRICT/트리거는 거부 게이트로 활용). **★ FK 거부코드 매핑: RESTRICT/FK위반=23503, 커스텀 트리거 RAISE=지정한 SQLSTATE(여기 0A000)** → 각각 409. **★ 삭제 거부 메시지는 도메인어로 사용자에게**(내부 테이블/제약명 노출 금지, client 는 서버 메시지 표시).

2026-07-14(1) 라벨링 사람 판정 문구·촬영환경 UX 개선(enum 무변 표시계층) | 작업: 설계문서(`2026-07-14-labeling-human-judgment-ux-copy-design.md`) 구현 — 저장 enum·DB·API 무변경, 화면 문구/상태만. ①`labelingDisplay.ts` ACTION_LABELS 3종(basking→휴식·eating_paste→슈퍼푸드 자율급여·eating_prey→곤충 사냥)·TARGET_LABELS 2종(tool→급여 도구(숟가락·주사기·핀셋)·object→일반 사물(장식물·은신처·나뭇가지)) 명칭 교체, PRIMARY_HELP basking 신설+moving/eating_paste/eating_prey 재작성(그림자·조명 노이즈↔실제 이동 대비), hand_feeding target 질문 '도구'→'급여 도구', 새 상수 4종(CONTEXT_TAGS_TITLE '촬영 환경 (필수)'·HELP·NONE_LABEL '해당 없음'·TARGET_TOOL_OBJECT_NOTE), ②`labelingV2.ts` `context_tags_not_selected` client 규칙(explicitlySelected 기반)을 rule 1 블록에 배치해 **absent 포함 전 경로에서 촬영 환경 직접확인 강제**(사용자 수정 — absent 조기반환보다 앞, server 빈배열 계약 불변), ③`_labeling-forms.tsx` 촬영환경 영역 제목(필수)+안내문+`해당 없음` 버튼(태그와 상호배타, contextConfirmedNone=직접선택∧빈배열)+사물/급여도구 경계 안내문(tool·object 동시 후보일 때만), ④사용자 노출 '도구'→'급여 도구' 통일(PRIMARY_HELP.hand_feeding·hand-feeding 근거 안내문·체크리스트·hand_feeding_target 오류 4곳). TDD: 테스트 먼저(1차 RED 10, 수정 2차 RED 3)→구현→GREEN. web 263 tests·tsc·git diff --check·사용자 터미널 production build 통과(Next.js 14.2.35, static pages 18/18). | 참조: general#1,2,3,7,12 + test-driven-development | 지킴: **TDD(문구 회귀·context_tags_not_selected·빈배열 통과·hand_feeding+해당없음 §7.3 test-first, RED 확인 후 구현)**, **general#1(labelingDisplay/labelingV2/forms/correction-panel/3 page patchGt 다 Read 후 구현, explicitlySelected 자동추가 계약 확인)**, **general#2(공유 GroundTruthForm 확장·enum/DB/API 경계 유지·최소변경, 무관 리팩터 0)**, general#12(web 명령 전부 `cd web` 프리픽스), **다른 세션 겹침 방지(§10: git status 선확인, §9 대상 5파일 미커밋 0 확인, experiments/next-session 미접촉)**, 기존 테스트 1개(honors explicit selection)만 새 계약 반영 갱신. | 놓침: -. | 재발: -. | 메모: **★ '해당 없음'은 저장 enum(ContextTag)에 none 추가 없이 UI 전용 상태**(빈 배열[]+explicitlySelected.has('context_tags')로 "미확인 vs 확인후 없음" 구분 — patchGt 가 note 제외 키를 자동으로 explicitlySelected 에 넣는 기존 계약 재사용). **★ 표시 문구는 공통 표시계층(labelingDisplay) 단일 SOT, forms 하드코딩 금지**(production·tutorial·correction 자동 공유). **★ enum rename 없이 화면 명칭만 바꾸는 UX개선=온톨로지 마이그와 구분**(basking 저장키 유지, 화면만 '휴식'). **★ 촬영 환경(필수)은 absent 포함 예외 없이 client 직접확인 강제**(사용자 승인 스펙 — 초안의 "non-absent 만/absent 스코프밖" 판단은 기각됨). 구현 급소: `collectGroundTruthIssues` 의 context 미선택 검증을 **absent 조기반환보다 앞(rule 1)** 에 둬야 absent 도 걸린다. server 빈배열([]) 허용 계약은 그대로. **★ Claude 세션 build 훅 차단 후 사용자 터미널 production build로 최종 검증**(Next.js 14.2.35, compile·lint/type·page data·static pages 18/18·traces 완료).

2026-07-15(1) 라벨링 후보 격리함 구현(triage quarantine, DB+API+큐필터+UI) | 작업: 계획서(`2026-07-15-labeling-triage-quarantine.md`) Task 1~7 — ①`2026-07-15_labeling_triage.sql`: `clip_labeling_triage`(owner_decision all-null/all-set CHECK, effective-state 인덱스)+`clip_labeling_triage_events`(FK 없이 clip_id만=원본 삭제 뒤에도 감사 보존, UPDATE/DELETE/TRUNCATE 트리거 차단 0A000)+service_role 전용 RPC 3(suggestion upsert=owner결정 미덮음·세션있으면 quarantine 거부 label fail-open·IS NOT DISTINCT FROM 비교·변경시만 이벤트 / decide=label/skip/reset·expected_updated_at stale_state·skip+세션 labeling_started / manual quarantine=owner결정 초기화·not_labelable), 전부 SECURITY DEFINER search_path=public,pg_temp, 라이브DB 사전조건 read-only 선확인(camera_clips/sessions 존재·컬럼), **미적용**(승인 후 apply+rollback probe). ②공유 순수 `labelingTriage.ts`(effectiveTriageState owner>시스템·triageReasonLabel 사전정의 3문구만)+server `labelingTriageServer.ts`(cursor base64url 엄격디코드·owner-safe 매퍼가 evidence_snapshot/checkpoint/host 비노출) TDD. ③`labelingQueue.ts` fetchTriage 콜백(Promise.all 배치동시조회·completed 먼저·pending/skipped 제외·전역 NOT IN 금지)+queue route triage 조회(실패 throw→502 fail-open 안함). ④owner API 4(list/detail/PATCH decide/manual POST, 전부 requireOwner·UUID/enum/limit/cursor 검증·도메인코드 정확매핑 404/409/409·DB오류 databaseUnavailable 일반502). ⑤client `getTriagePage/getTriageDetail/decideTriage/manualQuarantineClip`(ApiError에 code 추가해 409 stale_state↔labeling_started 구분), categorize/redirectTarget를 `labelingRouteAccess.ts` 순수모듈 추출(격리함=owner 카테고리→labeler URL직입 /labeling 튕김)+격리함 nav. ⑥격리함 3탭 목록+상세(헤더→영상→결정→provenance, auto-next, stale 리로드·labeling_started 안내)+본큐 owner `격리함으로` 버튼(preventDefault/stopPropagation·confirm·로컬제거). ⑦DATABASE/FEATURES/next-session/donts-audit SOT 갱신. 검증 web 320 tests·tsc·Python 336·git diff --check clean·보안 audit(응답 evidence/DB메시지/비밀값 0). | 참조: general#1,2,3,6,9,11,12 + test-driven-development + security-review + 적용마이그=후속원칙 | 지킴: **TDD 전 계층(effectiveTriageState 5케이스·cursor 3거부·safe매핑 evidence비노출·큐 7매트릭스·route 33 owner/검증/충돌 test-first RED→GREEN)**, **general#1(labelingQueue/labelingAccess/apiErrors/router-review route/labelingApi/layout/Button/PlaybackUrl 다 Read 후 구현, 마이그 append-only·RPC 패턴 미러, 라이브DB 스키마 선검증)**, **security(RLS ON+client정책0+service_role 전용, 이벤트 append-only 트리거로 service_role도 차단, owner-safe 매퍼로 evidence_snapshot 절대 미노출, DB오류 databaseUnavailable 일반502·error.message 미노출, requireOwner 진입점 전수)**, **적용마이그 미수정→신규 forward-only 파일**, general#2(collectQueuePage에 fetchTriage additive·기존 2테스트 보존·layout 순수함수 추출은 최소), general#12(web 명령 전부 `cd web` 프리픽스·pytest만 루트), **fail-open 금지(triage 조회 실패=502, skip clip 재노출 방지)**, migration static verify(whitespace+보안마커). | 놓침: -. | 재발: -. | 메모: **★ 계획서에 "production migration 미적용·commit·push 금지, 사용자 검토에서 멈춤" 명시→코드+테스트만, apply/deploy/커밋은 승인 경계**(설계 §11 순서: migration→preview→prod→worker preview30→canary→backfill). **★ 감사 이벤트는 FK 없이 clip_id만**(FK CASCADE면 append-only 차단트리거가 부모 삭제까지 막음 — 원본보다 오래 보존 의도). **★ optimistic concurrency=expected_updated_at 불일치 stale_state 409**(stale 브라우저 탭 결정 거부). **★ 도메인 충돌은 RPC가 jsonb {ok:false,code}로 반환·구조오류는 RAISE SQLSTATE**(route가 code→404/409, 22023→400, 그외 502). **★ ApiError에 code 필드 추가로 같은 409 status를 UI가 분기**(stale_state 리로드 vs labeling_started 목록복귀). **★ npm run build는 레포 훅 차단→tsc만, 실빌드 사용자 터미널.**

2026-07-15(2) 라벨링 격리함 2차 하드닝(세션 양방향 원자성·GT격리·필터·상세UX·라벨가능계약) | 작업: 사용자 리뷰 6항목(미적용 migration 내부 수정) — ①세션↔격리 양방향 원자성: production `clip_labeling_sessions`에 authenticated 자기-INSERT RLS가 있어 큐 필터만으론 부족 → `fn_guard_labeling_session_vs_triage`(SECURITY DEFINER, triage REVOKE ALL 읽어야 함) BEFORE INSERT OR UPDATE OF clip_id 트리거로 quarantine/skip clip 새 세션 생성 `PT409` 차단(clip_id 무변 UPDATE는 skip), RPC 3개+트리거 lock 순서 `camera_clips FOR UPDATE→triage FOR UPDATE` 통일(FOR KEY SHARE 폐기), rollback probe에 세션 INSERT 차단/허용 5케이스 추가 ②GT 저장 API(`/api/labeling-v2/[clipId]/gt`) 저장 전 `loadTriageEffectiveState` 사전검사 `409 triage_quarantined`+세션 INSERT `PT409` 최종안전장치 매핑(throw new Error로 code 소실 방지), `_helpers.databaseError` supabase 원문→일반 502(console.error 로그만) ③list API `date_from/date_to/camera_id` 검증(400)+list/count/cursor/상세 next 동일 적용(공유 `applyTriageStateFilter/applyTriageClipFilters/parseTriageClipFilters` server 모듈), count는 `camera_clips!inner` embed로 필터, `fn_triage_camera_options()` DISTINCT RPC+`/cameras` route(product owner 카메라 아닌 triage 대상만 — 테스트계정 분리) ④상세 clipId 전환 시 item/blocked/notice/err/video 초기화(같은 인스턴스 재사용), 영상 발급/재생 실패 명확 표시+재시도, `영상 ready 전 label/skip 비활성`(onLoadedMetadata=ready), URL state≠실제 effective state `409 state_changed`(client 목록복귀), 필터 URL 상세·다음·복귀 유지 ⑤upsert/manual 모두 `has_motion=true AND r2_key NOT NULL` 아니면 `not_labelable` fail-closed ⑥신규 테스트+문서 SOT 정정(설계 "구현 전 검토"→"코드·2차 하드닝 완료/미적용"). 검증 web 337(신규 13: gt 409×3·databaseError·detail state_changed·필터 3·cameras 3·parseFilters 4)·tsc·Python 336·whitespace·보안audit(evidence/DB원문 0). | 참조: general#1,2,3,6,9,12 + test-driven-development + security-review + 적용마이그=후속원칙(단 미적용이라 원본수정 허용) | 지킴: **미적용 migration은 원본 수정(적용됐으면 후속 — 상태 구분)**, **production RLS 실측(authenticated 자기-INSERT 정책 확인 → 큐 필터만으론 부족 판단 → DB 트리거)**, **lock 순서 통일로 deadlock/경합 방지**(모든 관련 쓰기 camera_clips→triage), **security(가드 트리거 SECURITY DEFINER로 REVOKE된 triage 읽기, DB원문 databaseError 일반화, PT409 code 보존해 409 매핑, evidence 여전히 비노출)**, **API 사전검사+DB 트리거 이중방어(사전검사로 대체 안 함)**, TDD(gt 격리 409·databaseError·detail state_changed·필터·cameras·parseFilters test-first), general#12(web 명령 `cd web` 프리픽스 — cwd 오염 1회 발생 즉시 교정), 공유 헬퍼 추출로 list/detail 필터 중복 제거. | 놓침: -. | 재발: **○ cwd 오염(pytest 루트 실행 후 npm이 루트서 돌아 ENOENT package.json) — 즉시 `cd web` 재실행. donts#12 정착됐으나 여전히 주의**. | 메모: **★ production RLS에 client 직접 write 정책이 있으면 서버 필터만으론 불변식 못 지킨다 → DB 트리거로 강제**(clip_labeling_sessions 자기-INSERT RLS가 격리 우회 가능 → 가드 트리거). **★ SECURITY DEFINER 트리거로 REVOKE된 테이블 읽기**(authenticated가 INSERT하는 세션 트리거가 REVOKE ALL된 triage를 읽어야 함). **★ lock 순서는 모든 관련 쓰기에서 동일하게**(camera_clips→triage — RPC/트리거 섞이면 반대 순서가 deadlock). **★ Supabase error를 `throw new Error(error.message)`하면 .code 소실**(도메인 SQLSTATE 매핑하려면 원본 error 객체 검사 또는 throw 전 code 분기). **★ 트리거 UPDATE OF col은 값 무변경도 발동**(gt resume이 clip_id 재set → `NEW IS NOT DISTINCT FROM OLD`로 skip). **★ 커스텀 SQLSTATE(PT409)로 트리거↔API 계약**(message 매칭 회피, donts vlm 원칙). **★ 필터를 embed 컬럼에 걸려면 count/next select에도 `!inner` 둬야 함**(head count도 parent 기준). **★ 상세 페이지는 동적 param 바뀌어도 언마운트 안 됨 → 상태 명시 초기화 필수**(blocked/notice 잔상). **★ owner 필터 옵션은 product-owner 소유가 아니라 실제 대상 집합에서**(테스트계정 분리 시 /labels/filter-options 재사용 금지).

2026-07-17 Python Evidence S1 recovery — A6 PATH 수정 + 완주 시도 → `S1R_HOLD_INCOMPLETE`(S2 blocked) | 작업: cross-repo handoff(Mac mini runtime). Task1 A6 확정원인 정정(실행 PATH `/opt/homebrew/bin` 누락, PYTHONPATH/temp 추정 additively 정정)+`verify_executable_dependency` fail-closed(ffmpeg_missing/ffmpeg_unusable, detector/R2/temp 전) TDD. Task2 success-only resume(error/warmup/무효 e2e 는 완료 아님·중복성공 BenchContractError)+완전성 계약(expected_measured_keys/completeness_report)+recovery TEST-SHEET PRE_REGISTERED. Task4 mini canary(6frames·temp0·detector/VLM0). Task5 MPS window1(333rec, A6 29/32 정상). **발견: 3 clip(dur5.2~5.5s) extract_six 결정론 실패** → read-only 6단계 진단(대표 `7af0e302` 확인: input-seek `-ss t -i`가 sparse-decodable clip(13 vs 32frame) 이른 timestamp rc=234; duration mismatch·clip-short 아님·keyframe 수 미측정. `8c472464`·`cfa27599` 는 3/3 동일 실패 확인·저수준 원인 추정) → 18 key 영구 미완성 → 동결계약 §9 PASS 원리적 불가 → halt. 독립재계산 harness 정확일치. | 참조: general#1,3,9 + handoff gate(HANDOFF_OK) + research-testing(TEST-SHEET/REPORT) + test-driven-development + systematic-debugging | 지킴: **verdict namespace 분리(S1R_*, original S1_* 미개명)**, **original 보존(throughput dir 삭제·재작성 0, recovery 빈 output 새 측정, partial 미복사)**, **production 코드 미수정(vlm_frames.py extract_six 손대지 않고 read-only 진단만)**, **동결 게이트 사후 변경 0(CROI cap 1435/h 상회해도 완전성 미충족→informative-only, 게이트 완화 안 함)**, **안전(temp0·DB/R2 write0·VLM0·LaunchAgent bootout/plist/env 0·production job 지연0·nightly/gate HEAD 불변)**, **flock 실측 후에만 lock-free flag(advisory lock 의미 확인)**, **안전창 <25분 NO_GO 준수(window2 미실행)**, systematic-debugging(사용자 지시대로 "sparse frame" 확정 전 read-only 6단계 진단으로 실제 원인=이른 timestamp input-seek 실패 규명, 초기 가설 정정). | 놓침: 초기에 "짧은 clip sparse frame(끝부분)" 오추정 → 진단서 이른 timestamp 실패로 정정(가설≠실측). plan 의 checkpoint 경로 stale(`/Users/baek-end/gecko-vision-gate` 없음→`myPythonProjects/gecko-vision-gate`) 실측 정정. | 재발: -. | 메모: **★ handoff 다른레포 runtime 은 verify_agent_handoff.py HANDOFF_OK 먼저·execution_repo HEAD==commit_sha 재확인.** ★ SSH 비로그인 PATH 는 `/opt/homebrew/bin` 없음→`shutil.which` None(A6 근본). ★ flock advisory lock=파일 존재≠held, ps/flock-probe 로 실측. ★ 결정론적 per-clip 실패는 retry 로 회복 불가→완전성 계약 영구 미충족→HOLD_INCOMPLETE. ★ summary.json 은 gitignore 아님(raw_results.jsonl 만 `experiments/**/*.jsonl` ignore)→resume 전 mv 로 worktree clean 유지(repo_dirty preflight 회피).

2026-07-17 Python Evidence S1R2 CROI 단독 합격시험 → `S1R2_PASS_CROI_THROUGHPUT`(S2 allowed·계획만) | 작업: cross-repo handoff(Mac mini runtime). 비교용 A6(CROI production path 선행의존 아님)를 CROI 게이트에서 분리, 질문을 CROI/MPS/cold 단독 처리량 하나로 좁힘. Task1 고정 profile `croi-mps-cold`(`ExecutionProfile`/`resolve_execution_profile` frozen, `--profile` CLI default full, cpu 요청 profile_device_mismatch fail-closed, A6 없으면 FFmpeg not_required — full 하위호환 보존) TDD. Task2 `expected_measured_keys` conditions/cache_modes 주입(기본=full)+`evaluate_s1r2_croi_gates`(성공 measured CROI/mps/cold 만, 우선순위 OPERATIONAL_RISK>MEASUREMENT_INTEGRITY>RELIABILITY>THROUGHPUT, 5 verdict·HOLD 없음, 운영안전 명시 주입) — 테스트 20개 RED→GREEN(568 pass). Task3 TEST-SHEET PRE_REGISTERED(runtime 전 push). Task5 Mac mini 한 안전창 완주(warmup32+measured96=128, 96/96 complete·dup/unexpected/error 0). Task6 독립 재계산 stdlib(counts·percentile 정확일치)+verdict. CROI cold p95 2.55s/cap 1411.69/h=gate160의 8.82배, RSS1.10GiB·temp16MB. | 참조: general#1,2,3,7,9 + handoff gate(HANDOFF_OK) + research-testing(TEST-SHEET/REPORT) + test-driven-development + using-git-worktrees | 지킴: **verdict namespace 분리(S1R2_*, S1/S1R HOLD 이력 additively 보존)**, **full profile 하위호환(default full·4조건·2캐시·A6 FFmpeg 요구 불변, 기존 호출자 signature 보존)**, **빈 dir 새 raw(S1/S1R raw 미복사·미import, 독립 재계산은 새 raw 하나만 read)**, **동결 계약 사후 변경 0(96/160/4GiB/2GiB 게이트 결과 본 뒤 불변)**, **안전(temp0·DB SELECT/R2 GET only·write0·VLM0·LaunchAgent/plist/env 미수정·production HEAD 3개 불변·router-features PID 무재시작·err Δ0)**, **flock lsof 실측 후 lock-free flag**, **증거 소실 금지(맥미니 untracked S1R recovery summary.json 이 incoming tracked 와 DIFFERS → worktree 밖 보존 후 ff)**, TDD(profile 계약·96-key·5 verdict·독립불일치·중복·자원천장 test-first). | 놓침: plan checkpoint 경로 stale(`/Users/baek-end/gecko-vision-gate` 없음→`myPythonProjects/gecko-vision-gate` canonical 실측 정정, S1R 과 동일). | 재발: **○ checkpoint 경로 stale(S1R 에서도 동일 정정) — plan 절대경로는 실측 확인 습관.** ○ summary.json/preserved 파일 untracked→repo_dirty preflight abort(S1R 메모 재확인, worktree 밖 mv 로 해소). | 메모: **★ 시험 계약을 질문보다 넓게 묶지 말 것**(S1R 은 비교용 A6 availability 를 CROI 채택 게이트에 결합→A6 결정론 실패가 CROI PASS 를 막음. profile 로 질문을 실제 production path 하나로 좁혀 해소). **★ 고정 profile = 임의 조합 CLI 대신 device·conditions·cache 를 불변 한 벌로**(YAGNI, 일반화 금지 하드계약). **★ gate 우선순위 명시**(운영 안전 위반은 성능 좋아도 override). **★ 운영 안전은 timing record 로 추론 말고 runtime audit before/after 로 명시 주입.** **★ ff-only merge 가 untracked 충돌로 abort 시 내용 DIFF 먼저**(동일이면 안전 제거, 다르면 worktree 밖 보존 — 증거 소실 금지).

2026-07-21 T0 bowl-dwell probe (체류=케어 신호 blind 선검증, 80건) → `reject`(체류-단독 무효) | 작업: production DB read-only SELECT → 그릇 셀 dwell 랭킹 top60+random20 → R2 다운로드 → owner blind 육안 판정 → 결정론 채점 → REPORT. LLM 0회. TDD 5테스트(draw_grid_overlay/bowl_dwell_sec/is_eligible/sample_split/score_groups) RED→GREEN. STOP 3곳(시험지 승인·그릇 셀 지정·육안 판정) 사용자 대기 준수. | 참조: general#1,3,11 + research-testing(TEST-SHEET 사전등록·사후 변경 금지·decision 기계적용) + test-driven-development + python(#11 절대경로·#13 fake numpy 프레임) + 하드계약 5(SELECT만·blind·시험지 동결·storage/·LLM0) | 지킴: **production DB SELECT만**(스크립트에 insert/update/delete/upsert/rpc 토큰 자체 부재), **blind 유지**(시트/파일명에 clip_id/dwell/group/VLM라벨 0 노출, 매핑키 key/ 격리·채점 전 미개봉, grep leak 0 자체검증), **시험지 동결**(§5 숫자 승인 후 불변, 채점 1회만), **storage/ 하위 저장**(clips/frames gitignored), **LLM/VLM 0회**(owner 육안만), TDD(구현 전 실패 테스트 확인). | 놓침: 사용자가 그리드 notation 몰라 AskUserQuestion dialog dismiss → 프레임 1장씩 보여주며 대화형 셀 지정으로 전환(red-circle 표기 수용). | 재발: -. | 메모: ★ .numbers 는 zip(protobuf)이라 Read 불가 → `uv run --with numbers-parser`(프로젝트 의존성 미오염) 로 파싱, verdict 대소문자 정규화(Elsewhere→elsewhere) 후 표준 CSV 재작성. ★ blind 실험 핵심 발견: 체류 상위군은 그릇 근접(dwell median 34.3s vs random 0.0s)은 정확히 잡으나 케어행동 0/59 · near_bowl_no_care 20건 = **VLM confabulation 동일 실패모드를 비-VLM 시간축 신호에서도 재현**. reject 는 체류-단독 기각(결합 feature 여지 남김). ★ eating 은 owner "밥그릇 다른 날 배치"라 top군 물그릇 편중 → drinking 만 실질 검증(구조적 한계 REPORT §5 기록).

2026-07-21 연구방향 재정렬(P1/P2/P3) + T1 highlight-selection probe (합성점수 vs 무작위 blind 40건) → `reject`(점수식 v1 무효) | 작업: 4게이트 판정 로그 append(P1 adopt/P2 hold/P3 adopt) → P1 cross-repo 핸드오프(nightly-reporter plan/design 커밋 46ca39e + manifest, validator HANDOFF_OK) → T1 시험지 pre-reg·owner 승인·동결 → TDD 7테스트 RED→GREEN → 풀 1390 랭킹(S20+R20, 버킷캡4) → R2 40클립 → owner blind 판정 → 결정론 채점 1회 → REPORT/INDEX/decision-gate/next-session 갱신. LLM 0회. | 참조: general#1,2,3,11 + decision-gate 4게이트 + research-testing + test-driven-development + cross-repo handoff gate + 하드계약 5(T0 승계) | 지킴: **SELECT만**(쓰기 토큰 grep 0), **blind**(시트/파일명 clip_id/점수/그룹 0노출·key 채점 전 미개봉·uuid leak grep 0), **시험지 동결**(게이트 숫자 사후 불변 — S absent 30% 안전점검도 사전 정의대로 발동 기록), **채점 전 판정 룰 기계화**(decide() 테스트 선행), **handoff validator 통과 후 전달**(HANDOFF_OK 전문 기록), 조기 커밋+push(동시세션 pid 37571 공유트리 4회 분할 커밋). | 놓침: 테스트 픽스처가 버킷 3개×캡4=12 상한으로 top20 못 채우는 설계 결함 → 구현 아닌 픽스처 수정(버킷 7개). nightly-reporter 커밋이 main 아닌 feat 브랜치에 올라감(체크아웃 상태 미확인) → manifest에 브랜치 명시로 보완. | 재발: -. | 메모: ★ T1 핵심 발견: detector v2 오검출이 존재(observed_sec)+주기성(peak_autocorr) 성분을 **동시 오염** — S-absent 5/6이 Cam 2 동일 시그니처(전체관찰 65~69s·autocorr 0.8) = 카메라 고유 상시 오염원. **T0 "존재 신호 유효"는 Cam 2 제외 표본 덕(카메라 의존) — 부분집합에서 유효한 신호의 일반화는 별도 검증 필수.** ★ R base rate(informative 45%)가 T0 참고치 기대(낮음)와 크게 달랐음 — 풀 구성(카메라·기간) 바뀌면 base rate 재측정이 게이트 산정의 전제. ★ blind GT 누적 120건(T0 80+T1 40) = Data Engine v1 적립 부산물.

2026-07-22 라벨링 큐 최신순 보장(복합 cursor·keyset·stale 차단, cross-repo handoff) → `LABELING_QUEUE_NEWEST_READY_FOR_DEPLOY_REVIEW` | 작업: Codex handoff(`2026-07-22-labeling-queue-newest-order`) 계획 Task 1~5 — ①versioned opaque cursor(`labelingQueueCursor.ts`, `{v:1,started_at,id}` base64url, decode실패/미지version/invalid ts·uuid→`InvalidQueueCursorError`) TDD ②`labelingQueue.ts` scan `nextCursor`를 string→복합위치 객체(마지막 보인 항목) ③route `.order(started_at desc).order(id desc)`+cursor시 `or(started_at.lt.C, and(started_at.eq.C,id.lt.Cid))`, decode를 DB접근 전에 배치해 `400 invalid_cursor`(502 databaseUnavailable catch 밖)·외부 next_cursor는 opaque 문자열만 ④client `mergeNewestQueueItems`(매 merge id dedup+최신순 재정렬 순수함수)+page.tsx requestGeneration 세대 guard(stale 응답은 items/cursor/error/busy/loaded 무변경, 필터변경/언마운트 cleanup으로 진행요청 무효화) ⑤FEATURES §11.6·donts-audit·보고서. task별 commit 분리(fcbe797/435897d/1c3d9ad/645e937). 검증 web 356·tsc 0·**npm run build ✓(owner 터미널, static 19/19·타입체크 통과)**·Python 660·git diff --check clean·privacy audit(prediction_snapshot/reasoning/clip_python_evidence/behavior_logs 신규노출 0)·migration/DB write/deploy 0. | 참조: general#1,2,3,12 + cross-repo handoff gate(HANDOFF_OK) + test-driven-development + 하드계약(SELECT·schema불변·blind GT 컬럼불변) | 지킴: **TDD 전 task RED→GREEN 커밋 분리(cursor 6·queue 4·route 6·merge 3 test)**, **general#1(route/labelingAccess/labelingQueue/labelingApi/requestGeneration 다 Read 후 구현, 기존 triage route 테스트 mocking 패턴 미러)**, **schema/migration 0·blind GT 응답 컬럼·triage·튜토리얼·owner/labeler 게이트 불변·behavior/VLM/evidence 필드 신규조회 0**, **cursor 원문/DB오류 비노출(일반화 400/502)**, **명시 파일만 커밋(handoff 범위)**, general#12(web 명령 전부 `cd web` 프리픽스). | 놓침: -. | 재발: **○ cwd 오염(worktree root `cd` 커밋 후 npm test가 엉뚱한 곳서 ENOENT package.json — 2026-07-15(2)와 동일 패턴). 이후 git은 `cd` 대신 `git -C`로 전환·web은 full `cd web` 프리픽스 고정. donts#12 정착돼도 반복 → git 명령은 `git -C` 습관화가 확실.** ○ npm run build 훅 차단(2026-07-15(1) 기록대로 tsc만, 실빌드 owner 터미널 `!` 실행). | 메모: **★ timestamp-only cursor=같은 초 clip 페이지 누락**(started_at 단일키는 동률 순서 비결정 → id DESC 복합키 keyset 필수). **★ opaque cursor 이중 표현**(내부 scan=복합 위치 객체, 외부 API=base64url 문자열 — decode 실패는 DB 오류와 다른 층위 400 invalid_cursor). **★ stale 응답 방어=요청 세대 guard**(await 후 isCurrent 아니면 어떤 setState도 금지, effect cleanup이 next()로 진행요청 무효화 — polling 없이 필터 경합 해소). **★ merge는 순수함수로 분리**(dedup+재정렬을 컴포넌트 밖 테스트 가능한 helper로). **★ `[...map.values()]` 스프레드는 tsconfig target 낮으면 TS2802 → `Array.from`.** **★ 배포 자체는 Stop Point — owner/Codex review + production smoke 뒤에만.**

2026-07-22(2) 라벨링 큐 최신순 Codex 독립리뷰 후속(F1 정렬·F2 cursor, cross-repo handoff) → `LABELING_QUEUE_NEWEST_READY_FOR_DEPLOY_REVIEW` 유지 | 작업: Codex 리뷰 결함 2건만 TDD 최소 수정 — **F1** `mergeNewestQueueItems` comparator가 `localeCompare(ISO문자열)` → fractional second(`.100000+00:00`)나 offset 다른 표기(`Z` vs `-05:00`)에서 시간순이 뒤집힘 → `Date.parse` epoch millisecond DESC + 동률시 id DESC + malformed(NaN) 결정론 fallback(raw string DESC→id DESC), started_at 값·API 응답 미변환 정렬만 수정. **F2** cursor UUID regex가 version nibble `[1-5]`·variant `[89ab]` 제한 → PostgreSQL uuid의 canonical UUIDv7을 서버가 만든 cursor를 다음 요청에서 스스로 400 → canonical `8-4-4-4-12` hex만 확인(version/variant 무제한, 길이·hex·구분자 오류는 계속 거부). 신규 테스트 +8(client fractional/tie-break/malformed fallback 3 + cursor UUIDv7 round-trip/malformed거부 5) RED(3 failed)→GREEN(17 passed). 검증 web 364·tsc 0·Python 660·git diff --check clean·**npm run build 훅차단→owner 터미널**. 순수 lib 2파일+테스트 2파일+보고서 additive 절+본 audit. migration/DB write/deploy/Evidence B 0. | 참조: general#1,2,3 + cross-repo handoff gate(HANDOFF_OK) + test-driven-development + research-testing(보고서 additive) | 지킴: **RED 먼저 고정 후 최소 구현(F1/F2 각 실패 재현→수정)**, general#1(Date.parse fractional/offset 동작 node로 실측 후 comparator 작성 — 기억 단정 금지), **최소 변경(comparator 함수·regex 리터럴만, 정렬 정본·cursor 계약·금지경계 불변)**, **명시 파일만 stage**, **보고서 additive(기존 절 미개편)**. | 놓침: -. | 재발: ○ npm run build 훅차단(원 작업과 동일, tsc만 in-session·실빌드 owner). | 메모: **★ ISO-8601 timestamp 문자열의 사전순(localeCompare) ≠ 시간순 — fractional second 자릿수·timezone offset 표기가 섞이면 문자 비교가 실제 epoch 순서를 뒤집는다. 시간 정렬은 반드시 `Date.parse`(또는 epoch)로, 문자열 비교 금지.** **★ PostgreSQL `uuid` 타입은 UUIDv7도 저장 — 애플리케이션 UUID validation을 version/variant nibble(v1~5)로 좁히면 DB가 만든 값을 스스로 거부한다. canonical hex 형식만 검증.** **★ malformed 입력에도 sort comparator는 NaN 반환 금지(정렬 결과 비결정) → 결정론 fallback 필수.**

2026-07-22(3) 라벨링 큐 마이크로초 정밀도 Codex 후속(P1 sub-ms 정렬·P2 cursor toISOString 절삭, cross-repo handoff) → `LABELING_QUEUE_NEWEST_READY_FOR_DEPLOY_REVIEW` 유지 | 작업: 표본은 초 단위지만 timestamptz 계약상 마이크로초 손실 선제 차단 2건 TDD — **P1** `mergeNewestQueueItems` comparator가 `Date.parse`(ms 3자리 truncate)만 비교 → `.123400Z`와 `.123499Z`가 동률(99µs 손실) → `(epoch ms, 밀리초이후 6자리 정수)` 복합키(fraction 9자리 right-pad 후 slice(3), Date.parse 반영분과 무겹침·BigInt 불필요), offset 동일instant는 id DESC·malformed는 결정론 fallback 유지, started_at 원문 미변경. **P2** `decodeQueueCursor`가 `new Date(t).toISOString()`으로 `.123456Z`→`.123Z` 절삭 → route keyset 경계 `.123000` 이동해 사이 행 skip → strict RFC3339 검증(`^date T time (.\d{1,9})? (Z|±HH:mm)$`+Date.parse finite) 후 **timestamp verbatim 반환**(UUID 소문자화 유지), strict가 정밀도+PostgREST filter 문법 문자/timezone 누락 동시 차단. route/queue 로직 무변경(이미 cursor.startedAt verbatim 사용). 신규 테스트 +10(client sub-ms 1·cursor round-trip 2+non-RFC3339 거부 6·route keyset 1) RED(7 failed)→GREEN(33 passed). 검증 web 374·tsc 0·Python 660·git diff --check clean·**npm run build 훅차단→미검증(Codex 실행)**. 순수 lib 2파일+테스트 3파일+보고서 §10 additive+본 audit. migration/DB write/deploy/Evidence B 0. | 참조: general#1,2,3 + cross-repo handoff gate(HANDOFF_OK) + test-driven-development + research-testing(보고서 additive·표본 vs 계약 구분 명시) | 지킴: **RED 먼저 고정 후 최소 구현**, general#1(Date.parse truncate vs round를 node로 실측 후 comparator 설계 — 반올림이었으면 double-count 버그, truncate 확인해 안전 판단), **최소 변경(comparator·validTimestamp·return 3줄, 정렬 정본·cursor 계약·route/queue 로직 불변)**, **명시 파일만 stage**, **보고서 additive(§9 미개편, §10 신설)**, **표본 vs DB 계약 구분 정직 보고(현재 버그 아님 명시)**. | 놓침: -. | 재발: ○ npm run build 훅차단(원 작업·F1F2와 동일, handoff가 "막히면 미검증 두고 Codex 실행" 명시 → 정직하게 미검증). | 메모: **★ `Date.parse`(및 JS Date)는 소수 초를 밀리초(3자리)까지만 반영(V8 truncate 실측) — 마이크로초·나노초는 소실. 시간 정렬을 마이크로초까지 하려면 밀리초 이후 자릿수를 문자열에서 따로 추출해 별도 정수로 비교(BigInt 없이). truncate라서 앞 3자리와 무겹침 — 반올림 엔진이면 double-count.** **★ `new Date(t).toISOString()`은 timestamp를 밀리초로 재직렬화 = 마이크로초 절삭. cursor/keyset 경계값은 원문 verbatim 보존해야 pagination gap 없음(재직렬화 금지).** **★ keyset cursor 값이 그대로 PostgREST filter 문자열에 들어가면 strict 형식 검증(RFC3339)이 정밀도 보존 + filter 문법 문자(쉼표·괄호) injection 차단을 겸한다.**

2026-07-22(4) `motion_clips` 네이티브 운영 라벨링 v3 구현(Task 0~10, cross-repo handoff, 격리 worktree) → `V3_PREVIEW_READY_FOR_DEPLOY_REVIEW` | 작업: legacy `camera_clips` v2·튜토리얼·과거 GT 보존하고 운영 라벨링만 `motion_clips` 정본으로 전환하는 v3를 독립 추가(TDD RED→GREEN task별 커밋). ①DB migration(4테이블 triage/events/sessions/revisions + service-role RPC 6개, 최신순 keyset·append-only 트리거·blind GT 불변·경합 PT409/PT410, 실제 처리 가능 카메라 옵션 RPC) ②순수 타입/서버 매퍼(raw provenance 미노출·prediction 선택 결정론·안정 SQLSTATE→공개코드) ③queue/cameras API(role-aware, owner_id 필터 없음·labeler=label만) ④detail/media API(GT잠금 전 VLM/evidence 은닉·r2_key 서버재조회·{url,expires_in}만) ⑤owner decision(actor=bearer·optimistic concurrency) ⑥blind GT/vlm-review/revise(GT 화이트리스트 주입차단·prediction snapshot 서버선택·자동라벨 mirror 없음) ⑦owner 전체영상 큐 UI(탭·필터 URL영속·마이크로초 merge 재사용·request generation) ⑧직접 라벨링 화면(phase 상태기계·VideoPlayer onLoadedMetadata 콜백 additive) ⑨`/labeling` env wrapper 전환(기본 legacy·legacy 큐 verbatim 이관·`/labeling/legacy`). Claude 종료 후 Codex 독립 리뷰에서 페이지 100행 sentinel 단절·media/locked SQLSTATE 충돌·labeler 카메라 필터 불일치 3건을 발견해 `5d8ab2f`에서 TDD 수정. 최종 검증 Python 687·Web 484·tsc 0·git diff --check clean·금지동작 실행코드 0. migration apply/deploy/main merge/env변경/canary 0. | 참조: general#1,2,7,8,9,11 + cross-repo handoff gate(HANDOFF_OK 전문 검증) + test-driven-development + using-git-worktrees(격리 worktree·별도 브랜치) | 지킴: **task별 RED→GREEN 커밋 분리·명시 파일만 stage**, **general#1(motion_clips/cameras/clip_vlm_jobs 실스키마를 information_schema로 실측 후 SQL 작성·기존 triage/sessions/revisions 마이그레이션+route 테스트 mocking 패턴 Read 후 미러)**, **general#7(legacy v2·튜토리얼·격리함 구조 불변, page.tsx는 verbatim 이관 후 env wrapper)**, **general#8(AI 생성 계획서 비판검수 — GroundTruthForm/validateGroundTruth가 raw cast라 주입 가능함을 발견해 화이트리스트 추가·decision optimistic concurrency에 state_updated_at 노출 필요를 계획 밖에서 보강)**, **general#11(비밀값 0·r2_key/DB원문/secret 응답 비노출)**, **격리 worktree라 멀티세션 파괴 위험 없음·조기 push로 보호**. | 놓침: Claude 자체 적대리뷰가 페이지 100행·SQLSTATE 충돌·labeler 카메라 필터 불일치를 놓쳐 Codex 독립 검수로 보완. | 재발: ○ npm run build 훅차단(donts#9, 2026-07-22(1~3)와 동일 — tsc --noEmit 통과로 대체, 실빌드 owner 터미널 확인 필요). ○ `[...new Set()]`/iterator 스프레드 TS2802(2026-07-22(1) `Array.from` 교훈 재적용). | 메모: **★ handoff Task 0 shared_web_gate=clear는 Task 9에서 재확인(2단 게이트) — 다른 세션이 공용 파일 안 건드리는지 origin+local ref 둘 다 봐야(미푸시 커밋 누락 방지).** **★ 계획서에 코드가 박혀 있어도 실 스키마/기존 validator 동작은 도구로 실측 — validateGroundTruth가 whitelist가 아니라 raw cast였음(주입 방어를 route에서 추가).** **★ RPC가 typed row 반환+예외(RAISE ERRCODE)로 오류 신호 → API는 error.code(SQLSTATE) 매핑(v2 triage의 jsonb {ok,code} 패턴과 다름). optimistic concurrency는 클라이언트에 version(updated_at) 노출이 전제.** **★ `limit+1` pagination은 저장소/RPC 상한보다 공개 limit을 반드시 1 작게 유지하고, role별 필터 옵션은 실제 queue 조건과 DB에서 동일하게 계산해야 한다.** **★ 배포 자체는 Stop Point — migration apply/deploy/canary는 별도 deployment handoff의 Gate A~F(Codex 독립 diff리뷰·preview probe·owner smoke·명시 승인).**

2026-07-22(5) 제외·보류 GT guard(motion 라벨링 v3 결함 수정, cross-repo handoff, 격리 worktree) → `MOTION_SKIP_GT_GUARD_VERIFIED` | 작업: owner가 상세에서 제외(skip)/보류(hold) 후 같은 화면 GT 저장 시 `fn_lock_motion_clip_gt`가 분류를 조용히 `label`로 되돌리던 결함(prod 6건 `owner_skipped→owner_started_labeling` 감사로 확인)을 3계층에서 닫음 — ①공유 규칙 `canWriteMotionGt`(unreviewed/label만)+상세 UI 게이트/안내 Card/결정 후 탭이동 ②forward migration `motion_clip_gt_decision_guard`가 owner hold/skip 잠금을 **세션 쓰기 전** `PT424`로 거부(원본 함수 byte-identical + guard 10줄만) ③API `PT424→409 decision_blocks_labeling`(원문 비노출)+페이지 lockGt catch reload. TDD RED→GREEN task별 commit(23d485c/c8327c8/2429fb1). web **490**·pytest **694**·tsc clean·독립 리뷰 7/7 PASS. supabase `apply_migration`(tracked)+**live rollback probe 4/4**(skip/hold=PT424 delta 0·unreviewed→label+gt_locked·label labeler→gt_locked)+**persisted RPC canary**(새 skip clip `594bc406…` PT424·side effect 0·복구 확인)·기존 6건 mutation 0(md5 지문 3회 동일). main FF `fc46da0`, Vercel `dpl_6mCv…` READY(`label.tera-ai.uk`). | 참조: general#1,2,3,9,12 + cross-repo handoff gate(HANDOFF_OK) + research-testing(probe) | 지킴: **원본 migration 무수정(guard=별도 forward `CREATE OR REPLACE`, diff=guard 10줄만)**, **기존 6건/session/GT/triage/event 무변경(지문 3회 동일)**, **`LABELING_QUEUE_SOURCE` 불변**, general#1(관련 파일 전부 Read 후 구현·기존 route test mocking 미러), 명시 파일만 commit. | 놓침: -. | 재발: **○ cwd 오염(Task2에서 root `cd` 후 vitest를 root에서 실행→`@/` alias `ERR_MODULE_NOT_FOUND`, 2026-07-13/15/22(4)와 동일 donts#12). web 명령은 이후 `cd web` 프리픽스 고정.** ○ `npm run build` 훅(donts#9) 차단→tsc 대체(owner 터미널 권장). ○ Codex/Gemini CLI 둘 다 인프라성 다운(버전 비호환/tier 폐기)→독립 Claude 서브에이전트 리뷰 대체. ○ 브라우저 canary 확장 미연결→persisted RPC canary 대체+UI 클릭 워크스루 미실행 정직 보고. | 메모: **★ 이전(라벨링 큐·v3) audit의 "배포=별도 handoff Gate A~F" 메모와 달리, 이 task는 handoff가 migration apply/deploy/canary를 명시 인가 → inline 수행. owner browser smoke만 확장 미연결로 잔여.** **★ live rollback probe: DO block에서 시나리오 실행 후 최종 `RAISE`로 강제 롤백+결과를 에러 메시지로 surface(MCP execute_sql은 NOTICE 미노출).** **★ supabase `apply_migration`은 자체 tx 래핑 → 파일 BEGIN/COMMIT 제거하고 DDL만 전달. 적용 후 `pg_get_functiondef` LIKE로 guard 존재 재확인.**
